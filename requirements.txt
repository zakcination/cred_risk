# Core (required)
PyYAML>=6.0

# PDF extraction (optional but recommended; DOCX/XLSX need no extra deps)
pymupdf>=1.23

# Outlook .msg email extraction (optional)
olefile>=0.46          # read the OLE2 compound file / MAPI property streams
compressed-rtf>=1.0    # decompress the RTF-HTML body when no plain-text body exists

# Tests (dev)
pytest>=7.0

# Local analyst scripts (scripts/) — optional, not needed for topic_classifier
# itself. pyodbc needs the "ODBC Driver 17 for SQL Server" system package
# separately (not pip-installable) for the DB-connected scripts.
pandas>=2.0
matplotlib>=3.7
pyodbc>=5.0
python-dotenv>=1.0
openpyxl>=3.1          # writeoff_restoration_scan.py — reads the local .xlsx archive
