# Как правильно репортить дефекты IT / автору mart

Не абстрактная методология — выжимка из того, что реально сработало и реально
провалилось в этом проекте. Два источника: (1) письмо Сайлау про
`Guarantees.g_reserves_kzt` (×100, отправлено 04.08, в активной эскалации), (2) его же
evidence-скрипты `payments`/`payments_wiring`. Плюс собственные грабли этого репозитория
(`FINDINGS.md` §2, §5, §9.4.1) — находки, которые пришлось **отозвать** после того как
они уже казались готовыми.

## Почему это важно делать строго

`FINDINGS.md` живо именно тем, что в нём есть секция 5 (DISPROVEN) и записи вида
«ПРАВКА (self-review)» и «ПОВТОРНО ОТКРЫТ» — находки, отозванные после того, как
второй проход (иногда независимый ревьюер) поймал ошибку в самой проверке, не в
данных. Пример 04.08 этой же сессии: L12.3 показал S03=127 196 «несопоставленных
строк» — цифра была реальной для запроса, но не для вопроса, который он должен был
закрывать: джойн шёл по `loan_id`, а не по каноническому `gid` (тот же класс ошибки,
что `FINDINGS.md` §9.4 п.3 уже поймал для pledges/DWH-15 — 367.7М оказались
артефактом того же неверного ключа). Разница между «нашли баг» и «нашли баг в
проверке» стоит времени IT-команды и доверия к следующему письму.

## Чек-лист ПЕРЕД тем как писать IT (5 пунктов, каждый уже стоил времени здесь)

1. **Ключ канонический, доказан покрытием, не именем колонки.** `la_loan_id`/`loan_id`
   систематически ненадёжны как per-source ключи в этом марте (S03 old↔new: 0
   совпадений; DWH-08: 70 дублей `l_loan_id` даже внутри `loans`). Единственный
   доказанный NEW-internal паттерн — `*_gid = l_gid AND *_source = l_source`
   (`FINDINGS.md` §1). Если чек джойнит по чему-то другому — сначала перегнать на gid,
   потом уже смотреть на число.
2. **NULL отделён от «отсутствует» и от нуля.** `NULL days_past_due` ≠ «нет
   просрочки»; `dlcr_gid IS NULL` ≠ «событие осиротело» (это «ключ не заполнен» —
   другой дефект, другой адресат). Если проверка не разделяет эти два случая явно —
   число неинтерпретируемо, что бы оно ни показывало.
3. **Число стабильно минимум в 2 независимых прогонах/методах**, прежде чем
   называть его `CONFIRMED`. `payments_wiring`-скрипт буквально хардкодит ожидаемые
   значения предыдущего прогона и сверяет («06_PRIOR_RESULT_REPRODUCTION_CONTROL») —
   это не бюрократия, это доказательство, что число не артефакт грязного среза.
   Отдельно ценно, когда два **независимо написанных** скрипта сходятся на одном числе
   (наш L1.1 и находка Сайлау про дубли `loans` — оба дали 70/24/1/0).
4. **Net и gross раздельно.** Чистая дельта может быть маленькой при больших
   встречных отклонениях (`L6.2` vs `L6.3` — ровно этот паттерн уже в самом скрипте).
5. **Валентность не присваивается без эталона.** «Новая ветка выше» не значит «новая
   неправа» — старая может недорезервировать. Присваивать вину только против
   бухгалтерского/IFRS 9 эталона (`CLAUDE.md`, антипаттерны). Если эталона нет —
   писать вопрос, не обвинение.

6. **Срез данных указывается для КАЖДОЙ таблицы, а не один на письмо.** В этом
   марте единой отчётной даты нет, и получатель об этом знает лучше нас — письмо
   с одной датой в шапке он прочитает как небрежность. Состояние на 09.08.2026:

   | таблица | срез | примечание |
   |---|---|---|
   | `loans`, `loans_active` | 2026-08-01 | |
   | `loan_account` | **S03 — 2026-08-01; S01/S02/S17 — 2026-07-01** | сетка сдвинута (T35) |
   | `pledges` | 2026-08-01 | единственный срез, истории нет |
   | `interest_rates` | 2026-08-01 | единственный срез |
   | `ratings` | 2026-08-01 | единственный срез, миграция невыполнима |
   | `repayment_schedule` | срезов нет | только будущие платежи; самая ранняя дата 2026-07-06 у всех источников |
   | `borrower` | 2026-08-01 | |

   Практическое следствие: если показатель считается по `loan_account` и
   `pledges` вместе — по трём источникам из четырёх обеспечение берётся на дату,
   которой в портфеле нет, и это надо назвать в письме самим. Полный ряд срезов
   даёт `sql/DWH_PROBE_P12_SNAPSHOT_INVENTORY.sql` (P12d).

## Структура письма (реверс-инжиниринг письма про Guarantees)

1. **Точный скоуп**: schema.table, source, отчётная дата/период, объём (сколько строк
   проверено) — чтобы получатель не мог сказать «у меня по-другому», не проверив тот
   же срез.
2. **Правило, которое нарушено**, как формула, не как «не сходится»:
   `g_reserves_kzt = g_reserves_currency × 100`.
3. **Количественный эффект**: и агрегат (49.2 млрд → 492 млн), и системность
   («отклонений нет, за исключением округления» — т.е. не выборочный шум).
4. **Независимая перекрёстная проверка ВТОРЫМ методом/полем.** Guarantees: сумма
   ÷100 сходится не только по абсолютной величине, но и по проценту
   (`g_ifrs_reserves_percentage`) на конкретных БИН — два независимых пути к одному
   выводу. Это то, что превращает `hypothesis` в `proven`/`verified` на холсте.
5. **Явно, чего НЕ знаем.** `payments`-скрипт прямо пишет: без transaction ID нельзя
   отличить техническое дублирование от повторных реальных операций — это не
   слабость находки, это честная граница того, что доказуемо средствами целевой
   таблицы.
6. **Конкретный список просьб**, не «разберитесь»: проверить алгоритм/маппинг,
   подтвердить источник множителя, устранить дефект, перезагрузить данные, объяснить
   семантику поля.
7. **Ограничение на использование до исправления.** «Поле не может использоваться для
   расчёта провизий... до устранения дефекта» — явно, а не подразумевается.
8. **Воспроизводимый SQL приложен** — получатель не обязан верить на слово.

## Инвентарь: репортили vs накопили и не отправили (по состоянию на 04.08.2026)

| # | Находка | Источник | Статус эскалации |
|---|---|---|---|
| 1 | `Guarantees.g_reserves_kzt` ×100 | Sailau → Kadirzhan, письмо 04.08 13:33, эскалация Akberdiyev 16:15 | **Отправлено, в работе** — ничего не требуется с нашей стороны |
| 2 | `loans` дубли gid по всем источникам (S01=70/S02=24/S03=1/S17=0) + эффект на `repayment_schedule` | Наш L1.1 + независимо Sailau, числа совпали | **Отправлено** — письмо №19, 04.08 17:51, Sailau → Kadirzhan, cc Miras/Diana/Damir/Sabila/Yerlan |
| 3 | `payments` точные дубли (94 069 избыточных строк, до 2.87 млрд ₸ сценарно) | Sailau, evidence-скрипт есть | **Отправлено** — письмо №18, 04.08 15:42 (тема упоминает только `payments_wiring`, тело покрывает обе таблицы), те же получатели |
| 4 | `payments_wiring` S02: формула `total` (254 021 строк, 384.46 млн ₸) + дубли (18 269 групп) | Sailau, evidence-скрипт с regression-контролем | **Отправлено** — то же письмо №18 |
| 5 | L0.2: 4 схемных type mismatch (borrower_id, collateral_id, loan_number, loan_id×repayment_schedule) | Наш L0 (`is_gate=1`, блокирует TRUST_LEVEL всех 13 слоёв) | **Отправлено** — письмо №21, 04.08 22:17, Miras → Kadirzhan (черновик 1 ниже, отправлен как есть) |
| 6 | S03 pledges по корректному ключу: 94 732 договора без залога (было 49 по DWH-15, ключ был неверный); + новая находка S01=3 566 тем же ключом | Наш L9.2 + `CASE_RUN_PLEDGES_009` (подтверждено вживую), закрывает `FINDINGS.md` §9.5 п.2 | Получатели письма от 17.07 (старое число «49») не уведомлены о коррекции — **не отправлено**, черновик 2 ниже (теперь включает S01 как отдельный п.2) |
| 7 | L12.3 restructuring↔loans по gid (сегодняшний фикс) | Наш скрипт, только что исправлен | Требует повторного прогона на живых данных перед любым письмом — **не готово к отправке** |
| 8 | Потеря 90+ при переходе (S02 135.8М + S03 957.5М + S17 272.2М ≈ 1.365 млрд ₸) | `FINDINGS.md` §6 (S17-часть, письмо №4) + наш L8.5/L8.5b (консолидация всех 3 источников) | **Отправлено** — письмо №22, 04.08 22:17, Miras → Kadirzhan (черновик 3 ниже, отправлен как есть) |
| 9 | `FINDINGS.md` §7 — 15 открытых вопросов, явно помечены «только человек / автор mart» | Накоплено за всю сессию | Большинство пунктов уже покрыты письмами №1/2/4/6/7/8 по отдельности; пп. 6/7/8/10/11/12/15 **не заданы явно ни в одном письме** — см. «Ещё не заданные пункты» ниже |
| 10 | S03 provisions +30.6 млрд (MATCHED) | `FINDINGS.md` §4, самая крупная находка проекта | Валентность сознательно НЕ присвоена (нет эталона) — **правильно, что не отправлено как обвинение**; но сам факт расхождения тоже нигде не эскалирован формально |
| 11 | S01 `c_collateral_id` коллизирует между разными объектами залога (100% из 265 конфликтующих пар — разный `c_bpm_object_id`); искажает АГРЕГАТНОЕ покрытие портфеля — 533,51% (vs 80-111% у S03/S17), 214 договоров несут 58,05% суммы залога (3,10 трлн ₸, >3× весь ссудный портфель S01) | `CASE_RUN_LOANS_PLEDGES_011` D4 + `CASE_RUN_PLEDGES_012` (RESULT 01-08) + `CASE_RUN_LOANS_PLEDGES_013` (портфельный эффект, независимая методология) | **ОТПРАВЛЕНО** — черновик 4 ниже (эскалирован 07.08.2026, FINDINGS §D4.2; факт отправки подтверждён Мирасом 08.08.2026, точные №/время письма — уточняются) |
| 12 | `interest_rates`: две шкалы ставки внутри ОДНОЙ кредитной программы (229 программ / 1 720 575 договоров) + `effective_rate = 0` как пропуск (S17 220 077 строк = 76%) + `l_rate` в `loans` содержит название программы вместо ставки | Пробы P2/P7/P8/P9 (`sql/DWH_PROBE_P7…P9*.sql`), FINDINGS §11.1/§12/§13/§14 | **Готово к отправке** — черновик 5 ниже. Внутренними средствами разведено всё, что разводится; остаток (42 программы, где ×100 не подходит, и 72 программы S17 целиком в доле) на данных не доказуем — нужен маппинг загрузки |
| 14 | **`loan_account` теряет 248 107 строк между срезами 2026-01-01 и 2026-02-01 по ТРЁМ источникам** (S02 −83,3%, S17 −24,9%, S03 −18,3%); у S02 то же было в феврале 2025 (−22,2%). Ряд немонотонный — состав загрузки менялся. Счётный слой покрывает **14,1% активной книги S02** и 85,8% S17. `loan_account` — единственная историзованная таблица марта | P12/P13 (`sql/DWH_PROBE_P12_SNAPSHOT_INVENTORY.sql`, `…P13…REST.sql`), H02d; FINDINGS §20–§21 | **НЕ ОТПРАВЛЕНО** — черновик 8, **готов**. Дополнено: историзована 1 таблица из 14; `writeoff` — 54 строки против 135 146 в старой системе |
| 15 | `guarantees`: соотношение ×100 из письма 04.08 на срезе 2026-08-01 **не воспроизводится** ни у одной из 2 595 строк | M22, FINDINGS §18.9 | **НЕ ОТПРАВЛЕНО** — черновик 7. Это вопрос об изменениях на их стороне, не отзыв находки |
| 13 | **Признака просрочки 90+ в DWH нет ни в одном поле, пригодном для всех источников.** `days_past_due` не заполнен у 87,9% остатка последнего среза (S02 — 100%, ни одного значения); `delinquency_bucket` заполнен, но имеет РАЗНЫЙ домен по источникам и не тождествен 90+. 13 112 договоров отнесены DWH к старшим корзинам просрочки и не имеют ни одного дня DPD. У S02 вдобавок нет ни одной строки в `pledges` | Проба P10 (`sql/DWH_PROBE_P10_S02_DPD_AND_PLEDGE_TYPES.sql`), FINDINGS §17; H15 — §16 | **НЕ ОТПРАВЛЕНО.** Приоритет выше остальных: блокирует регуляторную отчётность, а не отдельный показатель. Черновик 6 ниже |

Пункт 6 — черновик 2 ниже ещё не отправлен, по шаблону выше (Кому/Копия как в письмах №17–20).
**Внимание:** черновик 2 составлен на срезе 01.07.2026, которого в базе больше нет
(`risk_analytics` перезаписывается без историзации, FINDINGS §9.7). Перед отправкой —
либо пересчитать на текущем срезе, либо явно оговорить, что число не воспроизводимо
на сегодняшних данных; иначе получатель не сможет проверить цифру у себя.
Пункт 7 — жди повторный прогон. Пункт 9 (остаток) — список без полных писем, см. ниже.
Пункт 10 — жди эталон (бух/IFRS 9), не пиши как дефект, пока валентность не на чём присвоить.

Полный текст отправленных писем №21/22 — `sailau_to_akberdiyev.md` (несмотря на название файла,
теперь трекает и письма Мираса в той же эскалационной цепочке).

## Черновики писем (готовы к отправке после вашей правки)

### Черновик 1 — L0.2: несовместимость типов ключей (4 связки) — ОТПРАВЛЕНО (письмо №21, 04.08 22:17)

**Кому:** Ақбердиев Қадіржан
**Копия:** Закарьянов Мирас, Умарова Диана, Джакипбеков Дамир, Чакенова Сабила, Әріпбай Ерлан
**Тема:** Несовместимость типов ключей в Dictionaries.risk_analytics — 4 связки блокируют
доверие ко всем последующим слоям проверки

В рамках послойной проверки `Dictionaries.risk_analytics` (`INFORMATION_SCHEMA`, срез
01.07.2026, read-only) выявлены 4 связки ключей с несовместимыми типами данных:

1. `loans.l_borrower_id` (varchar) ↔ `borrower.b_borrower_id` (bigint)
2. `loans.l_collateral_id` (varchar) ↔ `pledges.c_collateral_id` (float)
3. `loans.l_loan_number` (varchar) ↔ `loan_account.la_dog_num` (nvarchar)
4. `loans.l_loan_id` (nvarchar) ↔ `repayment_schedule.rs_loan_id` (bigint)

**Эффект:** JOIN по этим парам требует неявного приведения типов — это (а) делает JOIN
non-sargable (практически подтверждено: коррелированный JOIN по паре 4 вызвал зависание
проверки на 2+ часа на прогоне 04.08.2026 до перехода на явный `CONVERT`), и (б) меняет
семантику сравнения — varchar `'007'` физически не равен bigint `7`, даже если по смыслу это
один номер.

**Просим:** подтвердить, преднамеренно расхождение типов или это дефект миграции; дать
каноническое правило сравнения для каждой из 4 связок (сырое или приведённое); рассмотреть
унификацию типов на уровне схемы, если расхождение непреднамеренно.

**До ответа:** все проверки по этим связкам помечены в нашем отчёте `TRUST_LEVEL=ДОВЕРЯТЬ
НЕЛЬЗЯ` — это архитектурный гейт на все 13 нижестоящих слоёв проверки, не 4 отдельных чека.

Приложен воспроизводящий SQL (`L0.2`, `sql/risk_dwh_layered_check.sql`, без PII).

---

### Черновик 2 — ПЕРЕПИСАН 09.08. Уточнение к письму от 17.07: залоги S03

> **Что случилось с прежней редакцией.** Она предлагала заменить число 49 на
> 94 732 — «договоры S03 с заполненным `l_collateral_id` без записи в
> `pledges`, посчитанные по правильному ключу». Прогон 09.08 показал, что
> проблема была не только в ключе, но и в **постановке вопроса**: критерий
> смешивал договоры, у которых залога нет ПО ПРОДУКТУ, с теми, у кого он
> должен быть и отсутствует. 94 732 — это в основном первые. Отправлять это
> число нельзя: оно верно арифметически и бессмысленно содержательно.
>
> Ниже — редакция на данных 2026-08-01, где эти два случая разведены.

**Кому:** Ақбердиев Қадіржан (и/или Sailau, если находка 17.07 — его собственная)
**Копия:** Закарьянов Мирас, Умарова Диана, Джакипбеков Дамир, Чакенова Сабила, Әріпбай Ерлан
**Тема:** Уточнение к письму от 17.07.2026 — залоги S03 и S01: снимаем прежнее число, даём разложение

---

**1. Скоуп и срез.** `Dictionaries.risk_analytics`: `loans_active` и `loans` на
**2026-08-01**, `pledges` — **2026-08-01** (единственный срез в таблице).
Периметр — активные договоры: S03 220 589, S01 4 656. Связь залога с договором
берётся по `pledges.c_loan_gid = loans.l_gid`; `c_collateral_id` как ключ не
используется (см. п.4).

**2. Отзываем собственное число.** В письме от 17.07.2026 (п.3) фигурировало
«49 активных договоров S03 с заполненным `l_collateral_id` без записи в
`pledges`». Мы готовили уточнение, повышающее его до 94 732. **Это уточнение
отзывается до отправки** — критерий смешивал два разных состояния. Просим
считать недействительными оба числа, 49 и 94 732.

**3. Что на самом деле по S03.** Из 220 589 активных договоров залог имеют
92 851, не имеют — 127 738. Разложение по продукту:

| продукт | активных | с залогом | без залога |
|---|---|---|---|
| CAR | 93 225 | 92 851 (99,6%) | **374** |
| DSL | 124 186 | 0 | 124 186 |
| POS | 3 178 | 0 | 3 178 |

**DSL и POS — беззалоговые продукты**, и 127 364 договора без залога у них не
дефект, а структура портфеля. Прежний критерий относил их к пропускам.

**Реальный вопрос — 374 договора CAR без записи в `pledges`** при 99,6%
покрытии остальных договоров того же продукта. Просим подтвердить, ожидаемо ли
это (например, залог ещё не оформлен), или это пропуск загрузки.

**4. `c_collateral_id` как FK не работает — и это не мешает.** У S03 18
активных договоров имеют заполненный `l_collateral_id`, которому не
соответствует ни одна строка `pledges`. У S01 таких **1 322**. При этом по
`gid` залог находится корректно: у S17 все 15 801 договора с залогом имеют
ПУСТОЙ `c_collateral_id`, и это не мешает связи.

Просим зафиксировать в документации марта, что **связь `loans` ↔ `pledges`
строится по `gid`, а `c_collateral_id` для этого не предназначен.** Тогда
класс находок «ID заполнен, залога нет» закрывается как несущественный, а не
воспроизводится каждым новым анализом.

**5. Осиротевших залогов нет.** Ни одной строки `pledges`, чей `c_loan_gid` не
находился бы в `loans`. Целостность в эту сторону полная.

**6. По S01 числа не приводим.** По этому источнику стоимость и
идентификация залога непригодны из-за коллизии `c_collateral_id`
(письмо от 07.08). Здесь указан только счётчик битых ссылок — величина, на
коллизию не опирающаяся.

Воспроизводящий SQL: `sql/DWH_SCENARIOS_L2A_MEDIUM.sql` (M08–M10),
`sql/DWH_SCENARIOS_L2B_MEDIUM.sql` (M24),
`sql/DWH_PROBE_P10_S02_DPD_AND_PLEDGE_TYPES.sql` (P10d).

---

### Черновик 3 — Потеря 90+ при переходе: консолидация S02/S03/S17 — ОТПРАВЛЕНО (письмо №22, 04.08 22:17)

**Кому:** Ақбердиев Қадіржан
**Копия:** Закарьянов Мирас, Умарова Диана, Джакипбеков Дамир, Чакенова Сабила, Әріпбай Ерлан
**Тема:** Потеря признака 90+ при переходе в новую ветку — консолидация по S02/S03/S17,
совокупно ~1,365 млрд ₸

Консолидация ранее направленной по S17 находки (письмо от 16.07.2026) с данными по S02/S03:
договоры, где старая ветка показывает признак 90+, а новая — нет (включая NULL).

| Источник | Договоров | Сумма, ₸ |
|---|---|---|
| S17 | 932 (247 → NULL, 685 → ≤90) | 273 000 212,61 |
| S02 | 1 542 | 135 810 925,60 |
| S03 | 187 | 957 547 690,51 |
| **Итого** | **2 661** | **1 365 358 828,72** |

**Регуляторная значимость:** прямое занижение NPL/Stage 3 при переключении на новую ветку без
исправления DPD.

**Просим:** подтвердить причину потери признака по S02/S03 (тот же механизм, что у S17 — смена
расчёта DPD между загрузками, — или иная причина); предоставить план исправления до перехода
новой ветки в регуляторную отчётность.

Приложен воспроизводящий SQL (`L8.5`/`L8.5b`, `sql/risk_dwh_layered_check.sql`).

---

### Черновик 4 — S01: коллизия идентификатора залога искажает агрегатное покрытие портфеля (3,10 трлн ₸ — втрое больше всего ссудного портфеля S01) — ОТПРАВЛЕНО

> **Статус: отправлено** (подтверждено Мирасом 08.08.2026). Точный номер письма и
> время отправки — уточнить и внести сюда и в `sailau_to_akberdiyev.md`.
>
> **Незакрытый хвост, если придёт ответ:** RESULT 08 (`CASE_RUN_PLEDGES_012`) — проверка
> гипотезы float-округления — на момент отправки НЕ прогнан. В письме это указано честно
> («число цифр на момент письма не подтверждено»), но если Кадиржан спросит про механизм
> или предложит своё объяснение — прогнать RESULT 08 до ответа, иначе спорим без данных.

**Кому:** Ақбердиев Қадіржан
**Копия:** Закарьянов Мирас, Умарова Диана, Джакипбеков Дамир, Чакенова Сабила, Әріпбай Ерлан
**Тема:** S01: `c_collateral_id` коллизирует между разными объектами залога — искажение
подтверждено на уровне АГРЕГАТА, не только отдельных строк

В рамках проверки кардинальности и покрытия `pledges` ↔ `loans_active` по подтверждённому
ключу (`pledges.c_source+c_loan_gid = loans_active.l_source+l_gid`, срез `c_reporting_date`/
`l_report_date` = 01.08.2026) обнаружено: у 567 пар (договор, залог) источника S01 — по
`c_collateral_id` — больше одной сырой строки в `pledges`. Дальнейшая проверка показала, что
это не техническое дублирование строк, а **коллизия идентификатора между генуинно разными
объектами залога**, и что эффект не ограничивается этими 567 строками — он материально
искажает АГРЕГАТНОЕ покрытие всего портфеля S01 залогом. S02/S03/S17 задвоенных пар не
показывают (для S03 — см. явную оговорку ниже, это не то же самое, что "подтверждённо чисто").

**Правило, которое нарушено:** `c_collateral_id` должен однозначно идентифицировать один
физический объект залога. На деле — коллизирует.

**Количественный эффект — уровень строк:**
- 567 пар из 7 441 связей S01 (7,6%) представлены более чем одной сырой строкой; избыток —
  3 084 строки из 10 525 (29,3% строк S01).
- Из них 479 (84,5%) — строки с РАЗНЫМ `c_collateral_value` на одну и ту же пару. Из 479:
  214 различаются датой оценки (правдоподобная история переоценок) — но **265 (46,7% от
  567) — дата оценки ТА ЖЕ, значение разное.** Историей не объясняется.
- Величина расхождения для этих 265: только 4 (1,5%) — в пределах 1% (шум округления).
  **175 (66%) расходятся более чем в 2 раза; 238 (89,8%) — на 10% и более.**
- **Независимый идентификатор объекта, `c_bpm_object_id`, отдельный от `c_collateral_id`:
  для ВСЕХ 265/265 пар (100%) конфликтующие строки указывают на РАЗНЫЙ физический объект.**
  Это не одна и та же вещь с несинхронизированной оценкой — это два разных объекта залога,
  которым присвоен один и тот же `c_collateral_id`.

**Количественный эффект — уровень портфеля (НОВОЕ, самое существенное):**
- Агрегатное покрытие (Σ`c_collateral_value` / Σ`l_loan_amount`) для S03 — 80,23%, для S17 —
  111,26%: правдоподобные цифры. **Для S01 — 533,51%.**
- Это не редкие выбросы: **384 из 2 189 обеспеченных S01-договоров (17,5%) показывают
  покрытие >300%**, против 0,09% у S03 и 2,2% у S17.
- **214 договоров (9,8% обеспеченных S01) несут 58,05% всей суммы залога S01 — 3,10 трлн ₸
  из 5,34 трлн ₸ — и это ровно те договоры, что уже отмечены коллизией идентификатора выше.
  3,10 трлн ₸ САМИ ПО СЕБЕ превышают весь ссудный портфель S01 (1,00 трлн ₸) более чем в 3
  раза** — величина, при которой "легитимное многообъектное обеспечение" перестаёт быть
  правдоподобным объяснением без независимого подтверждения.
- Коллизия шире единичных пар и по другому измерению: из 956 групп "общий залог под
  несколькими договорами" (легитимный паттерн, ранее сообщённый как L9.4/C2) — **99 (10,4%)
  также показывают разный `c_bpm_object_id`** между "делящими" договорами. Часть уже
  сообщённой статистики совместного обеспечения, вероятно, та же коллизия в другой форме.

**Независимая перекрёстная проверка.** Число 567/3 651/3 084 воспроизведено дважды
независимо написанными скриптами (`CASE_RUN_LOANS_PLEDGES_011` результат D4 и отдельный
`CASE_RUN_PLEDGES_012`, `MATCHES_PRIOR_FINDING`). Портфельный эффект (533%/17,5%/58%)
получен ТРЕТЬИМ, отдельно написанным скриптом (`CASE_RUN_LOANS_PLEDGES_013`) с независимой
методологией дедупликации — и совпадает по направлению и концентрации с находкой на уровне
строк, а не противоречит ей.

**Явно, чего НЕ знаем:**
- Механическая причина коллизии. Рабочая гипотеза: `c_collateral_id` хранится как `float`
  (аномалия типа зафиксирована ещё в прогоне №1, 31.07.2026) — float(53) точно хранит целые
  только до 2^53≈9×10^15 (16 цифр); прочие gid-подобные ID в этой схеме — 18 цифр. Прямая
  проверка величины `c_collateral_id` для конфликтующих пар (`CASE_RUN_PLEDGES_012`,
  RESULT 08) — в работе, число цифр на момент письма не подтверждено.
- Для S03 `c_bpm_object_id` заполнен на 0% — независимо проверить `c_collateral_id`
  ТАКИМ ЖЕ способом невозможно. "0 задвоенных пар" для S03 — непроверенное предположение
  о чистоте, не факт того же качества, что для S01.
- Стабилен ли `c_bpm_object_id` во времени на один физический объект, или переиздаётся при
  каждой переоценке — от этого зависит, полностью ли 533%/17,5%/58% объясняются коллизией,
  или частично отражают что-то ещё (см. приложенный SQL, раздел "контраргумент").

**Просим:**
1. Подтвердить, что `c_collateral_id` задуман как уникальный идентификатор объекта залога, и
   как соотносится с `c_bpm_object_id` (какой из двух — источник истины).
2. Прямо ответить на гипотезу float-округления (`c_collateral_id` — тип `float`, следует ли
   хранить как целочисленный тип без потери точности).
3. Учитывая масштаб (искажение агрегата в разы, а не только 567 строк) — просим приоритет
   выше обычного разбора очереди, желательно созвон, не только переписка.
4. Прислать уточнённый счётчик/агрегат после исправления.

**До ответа:** любые суммы `c_collateral_value` по S01 — не отдельные 265/567 строк, а ВЕСЬ
источник целиком — не использовать ни для какой цели (LGD, провизии, отчётность). Искажение
подтверждено на уровне портфеля, не только отдельных строк.

Приложен воспроизводящий SQL:
- `sql/CASE_RUN_PLEDGES_012_S01_DUPLICATE_LOAN_COLLATERAL_PAIRS.sql` — RESULT 01-05 (числа,
  глубина дублирования, идентичность/различие значения, разбор по дате, контроль
  воспроизводимости), RESULT 06-07 (величина расхождения, независимое подтверждение через
  `c_bpm_object_id`), RESULT 08 (проверка гипотезы float-округления).
- `sql/CASE_RUN_LOANS_PLEDGES_013_COVERAGE_AND_LTV.sql` — покрытие по количеству (B1) и по
  стоимости (B2a/b), концентрация искажения в уже помеченных договорах (B3), кросс-договорная
  проверка "общего залога" (A1b).

### Черновик 5 — `interest_rates`: ставка непригодна для агрегатов (две шкалы в одной программе), ГЭСВ отсутствует у двух источников

**Кому:** Кадиржан (автор/владелец марта)
**Копия:** Sailau, Diana, Damir
**Тема:** `Dictionaries.risk_analytics.interest_rates` — единица измерения ставки и семантика `effective_rate = 0`

---

**1. Скоуп.** `Dictionaries.risk_analytics.interest_rates`, отчётная дата **2026-08-01**
(единственная в таблице), все четыре источника, **6 970 984 строки**. Дополнительно
`risk_analytics.loans` на ту же дату, 8 259 299 строк.

**2. Что подтвердилось как рабочее** (чтобы вопросы ниже не выглядели как сомнение
в таблице целиком):

- Ключ: **`interest_rates.loan_id = loans.l_gid`** — совпадение **100,0000%** по всем
  четырём источникам. Grain чистый: `loan_id` уникален построчно.
- Альтернативный ключ `dlcr_dog_num` + `source` — тоже 100%, но у него есть одна
  коллизия в S01 (один номер договора на два разных gid), поэтому рекомендуем gid.
- Номинальная ставка по **S02** — единственный источник без смешения шкал
  (1 199 программ, все в диапазоне 12,00–39,90).

**3. Нарушенное правило — как формула, не как «не сходится».**

Внутри **одной кредитной программы** (значение `loans.l_rate`) поле `interest_rate`
принимает значения в двух несовместимых шкалах одновременно:

    для программы P существуют договоры с interest_rate ∈ (0; 1]
    и договоры с interest_rate ∈ (1; 100]

**Количественный эффект:**

| Источник | Программ с обеими шкалами | Договоров затронуто |
|---|---:|---:|
| S01 | 6 | 12 672 |
| S03 | 191 | 1 689 678 |
| S17 | 32 | 18 225 |
| **Итого** | **229** | **1 720 575** |

Это не выборочный шум: у S02 таких программ **ноль**, то есть загрузка умеет писать
одну шкалу — расхождение локализовано в трёх источниках.

**4. Независимая перекрёстная проверка вторым методом.**
Тот же дефект виден с другой стороны — через внутреннюю когерентность ставок.
ГЭСВ (`effective_rate`) включает комиссии и не может быть ниже номинальной. Однако
у **S03 на 1 039 284 строках** ГЭСВ ниже номинальной, и это число близко совпадает
с количеством строк, где `effective_rate` записан долей (**1 077 578**). Два
независимых признака — распределение по программам и экономическая невозможность —
указывают на одно и то же.

**5. Отдельный дефект: `effective_rate = 0` означает «не заполнено», а не «APR 0%».**

`interest_rate = 0` не встречается **ни в одной строке ни одного источника**.
При этом строк с `effective_rate = 0`:

| Источник | ГЭСВ = 0 | из них с положительной номинальной |
|---|---:|---:|
| S17 | **220 077 (76,3%)** | **220 077 (100%)** |
| S03 | 6 009 | 6 009 (100%) |
| S01 | 186 | 186 (100%) |

Совпадение ровное — **каждая** строка с нулевым ГЭСВ имеет положительную номинальную
ставку. Средний ГЭСВ по S17 из-за этого занижен примерно вчетверо (5,97 против ~24
на заполненном подмножестве).

**6. Чего мы НЕ знаем — честная граница.**

Мы **не можем** предложить правило исправления. Гипотезу «значение ≤ 1 умножить
на 100» мы проверили и она **не универсальна**:

- На **42 программах из 229** (18,3%) результат ×100 не попадает в диапазон
  «процентных» договоров той же программы — то есть там причина другая.
- У **S17 72 программы / 178 267 договоров** (68% источника) лежат в `(0;1]`
  **целиком**, без единого «процентного» значения. Это одинаково согласуется и с
  «программа хранится долей», и с «это реально ставки ниже 1% годовых». Средствами
  самой таблицы различить нельзя — нужен маппинг загрузки.

Слепое применение ×100 испортило бы эти программы необратимо и незаметно.

**7. Смежный вопрос по `loans`.** Поле **`loans.l_rate` (varchar(255)) содержит не
ставку, а наименование кредитной программы** — `021 Просто Ипотека (приобретение)`,
`Installment with grace`, `POS MIDDLE`. Числовое приведение даёт **0 успешных из
8 259 299** строк при любом варианте нормализации. Вопрос: это осознанный перенос
(тогда поле стоит переименовать) или ошибка маппинга при загрузке?

**8. Просьбы (конкретно):**

1. Подтвердить **единицу измерения** `interest_rate`, `initial_nominal_rate`,
   `effective_rate`, `initial_effective_rate` — доля или проценты, и одинакова ли
   она по источникам.
2. Дать **правило приведения** для 229 программ со смешанными шкалами, либо
   перезагрузить эти данные в единой шкале.
3. Подтвердить семантику **`effective_rate = 0`**: это «не рассчитано» или «0%»?
   Если первое — заменить на NULL, иначе любое среднее по ГЭСВ будет занижено.
4. Пояснить, почему **ГЭСВ отсутствует полностью у S02** (`effective_rate` и
   `initial_effective_rate` — 100% NULL) и `initial_effective_rate` у S17.
5. Подтвердить или исправить **35 строк** S03 со значением ровно `999` в
   `effective_rate` (и 35 в `initial_effective_rate`) — похоже на техническую
   заглушку. Значения в диапазоне (100; 999) мы считаем правдоподобными для
   короткого POS-кредитования и как дефект не заявляем.
6. Пояснить **покрытие**: ставка есть только у **48,92% активного портфеля**
   (S03 98,57%, S01 81,64%, S17 37,43%, **S02 8,86%**). Это ожидаемое состояние
   загрузки или незавершённая миграция?
7. Рассмотреть **переименование `interest_rates.loan_id`** — поле содержит gid,
   а не loan_id, и это уже привело к неверному выводу при проверке связки.

**9. Ограничение на использование до ответа.**

До подтверждения единицы измерения поля `interest_rate`, `initial_nominal_rate`,
`effective_rate`, `initial_effective_rate` **не могут использоваться для расчёта
средних ставок, маржи, ценового анализа и любых агрегатов по портфелю** ни по
одному источнику, кроме номинальной ставки S02. Фильтры вида «ставка выше X%»
также некорректны: они молча теряют договоры, записанные долей.

**10. Воспроизводимый SQL приложен:**
`sql/DWH_PROBE_P7_REAL_RATE_COLUMN.sql`, `sql/DWH_PROBE_P8_INTEREST_RATES.sql`,
`sql/DWH_PROBE_P9_RATE_KEY_AND_SCALE.sql` — read-only, `MAXDOP 1`, без PII.

---

### Черновик 6 — Признака просрочки 90+ нет ни в одном поле, пригодном для всех источников

> **Приоритет выше остальных черновиков.** Прочие дефекты портят показатель.
> Этот — закрывает целый класс отчётности: без сопоставимого признака 90+ не
> строятся ни обеспеченность просрочки, ни roll-rate, ни cure/re-default.

**Кому:** Кадиржан (владелец витрины)
**Копия:** Sailau, Diana, Damir
**Тема:** `Dictionaries.risk_analytics.loan_account` — каким полем DWH определяет просрочку 90+

---

**1. Скоуп.** `loan_account`, последний срез КАЖДОГО источника (S03 — 2026-08-01,
остальные — 2026-07-01), **384 508 договоров, 1 577,0 млрд ₸ остатка**.

**2. Вопрос одной строкой.** Каким полем считать просрочку 90+? В таблице их
два, они дают разные ответы, и ни одно не покрывает все источники.

**3. `days_past_due` — не заполнен у 87,9% остатка.**

| источник | договоров | поле заполнено | остаток | доля остатка без DPD |
|---|---|---|---|---|
| S01 | 4 838 | 478 (9,9%) | 622,5 млрд ₸ | 91,1% |
| S02 | 34 735 | **0 (0,0%)** | 2,0 млрд ₸ | **100,0%** |
| S03 | 254 338 | 53 592 (21,1%) | 831,2 млрд ₸ | 84,3% |
| S17 | 90 597 | 10 939 (12,1%) | 121,2 млрд ₸ | 96,5% |

По **S02** поле пусто буквально: `MIN`, `MAX` — NULL, `COUNT(DISTINCT)` = 0.

Там, где поле заполнено, оно нормальное: максимумы 5 979 / 5 557 / 1 716 дней,
253–2 822 различных значения. Вопрос не в качестве значений, а в их наличии.

**4. Почему это не объясняется правилом «DPD ставится только просроченным».**

Такое правило похоже на правду: в корзине 1 доля NULL 96–98%, в корзине 3 —
16–55%. Но даже если оно действует, поле не заполнено там, где просрочку
признаёт сам DWH:

| источник | договоров в корзинах 3/4 | из них **без DPD** |
|---|---|---|
| S01 | 918 | 534 |
| S02 | 2 010 | **2 010** |
| S03 | 49 682 | 8 115 |
| S17 | 10 460 | 2 453 |
| **итого** | **63 070** | **13 112** |

**13 112 договоров отнесены к старшим корзинам просрочки и не имеют ни одного
дня DPD.** Просим подтвердить или опровергнуть: NULL в `days_past_due` — это
«просрочки нет» или «значение не загружено»? От ответа зависит, можно ли вообще
пользоваться полем.

**5. `delinquency_bucket` заполнен лучше, но не заменяет DPD.**

- Домен разный по источникам: S01/S03 — {1,2,3,4}; S02/S17 — {1,2,3,NULL}
  (9 497 и 6 258 договоров с NULL-корзиной).
- Границы разные: у S17 корзина 2 заканчивается ровно на 90 днях; у S03 в
  корзине 1 есть договор с DPD 98, в корзине 2 — восемь договоров с DPD 91.
- Корзина 3 не тождественна «90+»: в ней 91 договор S01, 5 888 S03 и 854 S17
  имеют DPD ≤ 90.

Просим прислать **справочник значений `delinquency_bucket` по каждому
источнику** — какой диапазон дней означает каждое значение и почему у S01/S03
есть корзина 4, а у S02/S17 её нет.

**6. Что это уже стоило в цифрах.** Недопокрытие обеспечением по договорам с
DPD > 90 считается как 18,80 млрд ₸ (S03 + S17; S01 исключён из-за коллизии
идентификатора залога — письмо от 07.08). Это **нижняя граница**: как минимум
13 112 договоров в расчёт не попали. Выбор между `days_past_due > 90` и
`days_past_due - 1 > 90` дополнительно двигает результат на 33,7 млн ₸.

**7. Отдельно по S02.** У источника нет ни одного значения `days_past_due`, ни
одной строки в `pledges` и **ни одной строки в `repayment_schedule`** — три
основные кредитно-рисковые таблицы не содержат S02 вообще. В `loan_account` последнего среза 34 735 договоров на
1,95 млрд ₸ — при том что в `loans` у S02 порядка 604 тыс. договоров, а ряд
`loan_account` теряет 163 936 договоров в январе 2026. Просим подтвердить, что
загрузка S02 в `loan_account` и `pledges` полна, либо назвать перечень того, что
в неё не входит.

**8. Что нам нужно от вас.**
1. Семантика NULL в `days_past_due` — «нет просрочки» или «нет данных».
2. Справочник `delinquency_bucket` по источникам.
3. Какое поле считать нормативным для признака 90+ — и если оба непригодны, то
   каким источником банк закрывает эту отчётность сейчас.
4. Статус загрузки S02.

**Готовы предоставить SQL-скрипт целиком и переслать полный вывод.**

---

### Черновик 7 — Гарантии: отношение ×100 на текущих данных не воспроизводится

> **Это ВОПРОС, а не отзыв находки.** Ответа на письмо от 04.08 мы не получали,
> так что об изменениях на их стороне знать не можем — их могли внести и просто
> не уведомить. `Guarantees` истории не имеет, поэтому состояние на 04.08
> проверить нечем: наши данные говорят только про 09.08. Формулировка обязана
> это отражать — «у нас сейчас сходится, что изменилось?», а не «мы ошиблись».
>
> **Перед отправкой:** прогнать исходный скрипт от 04.08 как есть, без правок.
> Если он на сегодняшних данных даёт то же, что и M22, — письмо уходит. Если
> расходится — сначала разбираемся, почему две наши же проверки не сходятся.

**Кому:** Ақбердиев Қадіржан
**Копия:** Sailau, Закарьянов Мирас, Умарова Диана, Джакипбеков Дамир
**Тема:** К письму от 04.08.2026 (`g_reserves_kzt` ×100) — на срезе 2026-08-01 отношение не воспроизводится

---

**1. Срез.** `Dictionaries.risk_analytics.guarantees`, отчётная дата
**2026-08-01**, источник S01, **2 595 строк** — вся таблица.

**2. Что показывает текущая проверка.** Отношение `g_reserves_kzt` к
`g_reserves_currency`:

| соотношение | гарантий |
|---|---|
| ровно 1 (значения совпадают) | 1 439 |
| оба значения нулевые | 1 136 |
| иное | 20 |

**Ни одной пары с множителем 100.**

**3. Почему пишем, а не молчим.** В письме от 04.08.2026 сообщалось о
соотношении `g_reserves_kzt = g_reserves_currency × 100`. Ответа мы не
получали, и если исправление уже внесено — это нормальный ход событий, о
котором нас могли просто не успеть уведомить. Но эскалация продолжает идти по
показателю, который на сегодняшних данных сходится, и это надо снять.

Мы **не утверждаем, что находка от 04.08 была неверной.** У `guarantees` нет
истории — ни одного среза, кроме текущего, — поэтому состояние на 04.08 мы
проверить не можем в принципе.

**4. Вопрос.** Вносились ли между 04.08 и 09.08 изменения в загрузку или расчёт
`g_reserves_kzt` / `g_reserves_currency`? Если да — просим подтвердить, и мы
закроем пункт у себя. Если нет — расхождение между двумя нашими замерами
означает, что различалась выборка или методика, и тогда разбираться будем мы.

**5. Отдельно, безотносительно ×100.** У 1 136 гарантий из 2 595 (43,8%) обе
суммы резерва нулевые. Просим подтвердить, что нулевой резерв по гарантии —
допустимое состояние, а не признак незагруженного значения.

Воспроизводящий SQL: `sql/DWH_SCENARIOS_L2B_MEDIUM.sql` (M22).

---

### Черновик 8 — `loan_account`: обвал загрузки в феврале 2026 по трём источникам

> **ГОТОВ К ОТПРАВКЕ.** P13 закрыл последний открытый пункт: у
> `repayment_schedule` один срез, история отсутствует. Все утверждения письма
> подтверждены полным рядом срезов по всем 14 таблицам с отчётной датой.

**Кому:** Ақбердиев Қадіржан
**Копия:** Sailau, Закарьянов Мирас, Умарова Диана, Джакипбеков Дамир
**Тема:** `Dictionaries.risk_analytics.loan_account` — падение объёма загрузки в феврале 2026 (−248 тыс. строк) и покрытие активного портфеля

---

**1. Скоуп и срез.** `loan_account`, полный ряд: **19 месячных срезов
2025-01-01 … 2026-07-01** у S01/S02/S17 и **20** у S03 (до 2026-08-01).
Пропущенных периодов в ряду нет. Сопоставление с `loans_active` на 2026-08-01.

**2. Наблюдение — февраль 2026.** Между срезами 2026-01-01 и 2026-02-01 объём
падает по трём источникам одновременно:

| источник | 2026-01-01 | 2026-02-01 | изменение |
|---|---|---|---|
| S01 | 5 020 | 4 936 | −84 (−1,7%) |
| **S02** | 185 828 | **30 973** | **−154 855 (−83,3%)** |
| **S03** | 324 878 | **265 376** | **−59 502 (−18,3%)** |
| **S17** | 135 275 | **101 609** | **−33 666 (−24,9%)** |
| **итого** | | | **−248 107 строк** |

Обычный месячный дрейф в этом ряду — от −0,5% до −4%.

**3. Почему это не похоже на выбытие договоров.** Ряд не монотонный: после
февральского падения S02 держится на 28–31 тыс. пять месяцев, а на 2026-07-01
поднимается до 34 735 (**+23,7%**). S03 растёт на 2026-01-01 (+12,2%) и на
2026-07-01 (+6,9%). Счётный слой, из которого договоры только выбывают, не
растёт. Рост означает изменение состава загрузки.

**4. У S02 то же самое было в феврале 2025:** 256 887 → 199 792 (−22,2%), тогда
только по этому источнику. Совпадение месяца два года подряд — повод спросить
про регламент годового закрытия или архивирования, а не про разовый сбой.

**5. Что это означает сегодня — покрытие активного портфеля.**

| источник | `loan_account`, последний срез | `loans_active` на 2026-08-01 | покрытие |
|---|---|---|---|
| S01 | 4 838 (2026-07-01) | 4 656 | 103,9% |
| **S02** | **34 735** (2026-07-01) | **246 931** | **14,1%** |
| S03 | 254 338 (2026-08-01) | 220 589 | 115,3% |
| S17 | 90 597 (2026-07-01) | 105 622 | 85,8% |

**По S02 остаток задолженности и просрочка существуют для 14% активной книги.**
По S17 без счётной строки остаются около 15 тыс. активных договоров. У S01 и
S03 счётных строк больше, чем активных договоров, — там присутствуют неактивные,
это отдельный вопрос о периметре таблицы.

Оговорка: у S01/S02/S17 срез `loan_account` на месяц старше `loans_active`
(2026-07-01 против 2026-08-01). Месяц объясняет проценты, но не разрыв в семь раз.

**6. Почему спрашиваем именно сейчас.** Мы проверили **все 14 таблиц марта,
имеющих колонку отчётной даты**. Больше одного среза — только у `loan_account`:

| срезов | таблицы |
|---|---|
| 19–20 | `loan_account` |
| 1 (только 2026-08-01) | `loans`, `loans_active`, `pledges`, `borrower`, `Guarantees`, `ratings`, `interest_rates`, `repayment_schedule`, `restructuring_v2`, `collections`, `credit_lines`, `Offbalance`, `writeoff` |

Любой динамический показатель — vintage, roll-rate, cure-rate, поведение
просрочки — опирается только на ряд `loan_account`, и февральский разрыв
проходит ровно посередине наблюдаемого окна.

**6.1. Отдельно по журналам событий.** `restructuring_v2`, `writeoff`,
`collections`, `Offbalance` — журналы событий, и один срез означает, что мы
видим их текущий дамп. Если событие удалено или перезаписано на источнике,
отличить это от «события не было» нечем.

Наиболее наглядно на `writeoff`: **54 строки** на весь март, тогда как в старой
системе `spis_v_ubytok_CL` содержит **135 146** событий списания. Просим
подтвердить, что `writeoff` предназначен только для текущего среза, и указать,
где смотреть полную историю списаний.

**7. Что просим.**
1. Что произошло с загрузкой `loan_account` в феврале 2026: изменился состав
   выгрузки, периметр источников или это потеря данных?
2. Тот же вопрос по февралю 2025 для S02.
3. Регламент: сколько срезов удерживается, по какому правилу удаляются старые,
   планируется ли историзация `loans`/`pledges`/`ratings`.
4. Ожидаемое покрытие активного портфеля счётным слоем — 14% по S02 это норма
   или дефект загрузки?

Воспроизводящий SQL: `sql/DWH_PROBE_P12_SNAPSHOT_INVENTORY.sql` (P12a–P12d),
`sql/DWH_SCENARIOS_L3A_HARD.sql` (H02d).

---

## Ещё не заданные пункты FINDINGS.md §7 (короткий список, не полные письма)

Остаток открытых вопросов реестра, ни разу не заданный явно ни в одном из 19 писем:

- **п.6** S02 product-разметка — отброшена в миграции или берётся из справочника по gid?
- **п.7** Владелец mart-модели — организационный вопрос, не данные; решать не письмом-находкой.
- **п.8** Источник текущего провизионного отчёта — откуда 8,5 млрд по расторгнутым (двойной учёт?).
- **п.10** Resolution-пайплайн (`writeoff`/`collections`/`Offbalance`) — план покрытия S02/S17?
- **п.11** `writeoff` grain — 84 строки на весь S01+S03: «1 договор» или «1 кейс списания»?
- **п.12** S03 18771-валентность — атрибуция «1428=модель, 18771=ETL» не подтверждена (85%
  дельты внутри MATCHED, не population); наша находка, не Sailau — черновик письма не готов.
- **п.15** S17 ONLY_NEW 18771 (8,26 млн) сидит на 18 договорах из 10 979 — что в этих 18?

Каждый — кандидат на короткое отдельное письмо (2–4 абзаца по шаблону выше), если приоритет
подтверждён.

## Стратегии найти больше (если инвентарь исчерпан)

1. **Искать тот же класс бага в других местах.** ×100-масштабный дефект (Guarantees)
   почти никогда не бывает изолированным — стоит систематически сверить ВСЕ парные
   currency/percentage-поля схемы (не только `g_reserves_*`) на ту же кратность.
   Аналогично: exact-duplicate детектор (метод из `payments`/`payments_wiring`) ни разу
   не применялся к `collections`, `offbalance`, `bankrupt`, `credit_lines`, `ratings`,
   `interest_rates` — ни одна из этих шести таблиц не имеет своего дубль-чека.
2. **Механически проверить весь репозиторий на loan_id-vs-gid антипаттерн.** Мы
   поймали его дважды (pledges, restructuring_v2) за счёт того, что оба раза кто-то
   читал SQL построчно. Нужен систематический grep всех `JOIN`/`NOT EXISTS` на
   `loan_id`/`la_loan_id`/`l_loan_number` там, где рядом есть `gid`-колонка с тем же
   назначением — включая скрипты Сайлау, не только наш.
3. **Прогнать оставшуюся vacant-column программу.** `SAILAU_METHOD.md` уже
   спроектировал масштабирование на 158 непроверенных колонок (Batch 2+,
   `offbalance` — Tier A, кандидат №1) — SQL готов
   (`Risk_DWH_Vacant_Column_Retest_Pack_20260803.sql`), не прогнан.
4. **Reconciling-запросы из `FINDINGS.md` §9.5, ещё не выполненные**: S02
   `ONLY_OLD` (124 964) против DWH-04's «выпало из `loan_account`» (159 603) по
   общему `l_gid`; «68 vs 44» (наши vs DWH-02 SMART_CARD) — оба дёшевы, оба
   потенциально закрывают открытый вопрос данными, не оценкой.
5. **DWH-13/14/16 — независимая проверка ключевым методом по покрытию**, ещё не
   сделана (`FINDINGS.md` §9.2/§9.5 п.5) — по профилю Miras, до подписи под чужими
   числами в общем письме.
