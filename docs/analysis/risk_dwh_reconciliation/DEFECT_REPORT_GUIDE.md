# Как правильно репортить дефекты IT / автору mart

Не абстрактная методология — выжимка из того, что реально сработало и реально
провалилось в этом проекте. Два источника: (1) письмо Сайлау про
`Guarantees.g_reserves_kzt` (×100, отправлено 04.08, в активной эскалации), (2) его же
evidence-скрипты `payments`/`payments_wiring`. Плюс собственные грабли этого репозитория
(`FINDINGS.md` §2, §5, §9.4.1) — находки, которые пришлось **отозвать** после того как
они уже казались готовыми.

## Почему это важно делать строго

`FINDINGS.md` живо именно тем, что в нём есть секция 5 (DISPROVEN) и записи вида
«ПРАВКА (self-review)» и «ПОВТОРНО ОТКРЫТ» — находки, отозванные после того, как
второй проход (иногда независимый ревьюер) поймал ошибку в самой проверке, не в
данных. Пример 04.08 этой же сессии: L12.3 показал S03=127 196 «несопоставленных
строк» — цифра была реальной для запроса, но не для вопроса, который он должен был
закрывать: джойн шёл по `loan_id`, а не по каноническому `gid` (тот же класс ошибки,
что `FINDINGS.md` §9.4 п.3 уже поймал для pledges/DWH-15 — 367.7М оказались
артефактом того же неверного ключа). Разница между «нашли баг» и «нашли баг в
проверке» стоит времени IT-команды и доверия к следующему письму.

## Чек-лист ПЕРЕД тем как писать IT (5 пунктов, каждый уже стоил времени здесь)

1. **Ключ канонический, доказан покрытием, не именем колонки.** `la_loan_id`/`loan_id`
   систематически ненадёжны как per-source ключи в этом марте (S03 old↔new: 0
   совпадений; DWH-08: 70 дублей `l_loan_id` даже внутри `loans`). Единственный
   доказанный NEW-internal паттерн — `*_gid = l_gid AND *_source = l_source`
   (`FINDINGS.md` §1). Если чек джойнит по чему-то другому — сначала перегнать на gid,
   потом уже смотреть на число.
2. **NULL отделён от «отсутствует» и от нуля.** `NULL days_past_due` ≠ «нет
   просрочки»; `dlcr_gid IS NULL` ≠ «событие осиротело» (это «ключ не заполнен» —
   другой дефект, другой адресат). Если проверка не разделяет эти два случая явно —
   число неинтерпретируемо, что бы оно ни показывало.
3. **Число стабильно минимум в 2 независимых прогонах/методах**, прежде чем
   называть его `CONFIRMED`. `payments_wiring`-скрипт буквально хардкодит ожидаемые
   значения предыдущего прогона и сверяет («06_PRIOR_RESULT_REPRODUCTION_CONTROL») —
   это не бюрократия, это доказательство, что число не артефакт грязного среза.
   Отдельно ценно, когда два **независимо написанных** скрипта сходятся на одном числе
   (наш L1.1 и находка Сайлау про дубли `loans` — оба дали 70/24/1/0).
4. **Net и gross раздельно.** Чистая дельта может быть маленькой при больших
   встречных отклонениях (`L6.2` vs `L6.3` — ровно этот паттерн уже в самом скрипте).
5. **Валентность не присваивается без эталона.** «Новая ветка выше» не значит «новая
   неправа» — старая может недорезервировать. Присваивать вину только против
   бухгалтерского/IFRS 9 эталона (`CLAUDE.md`, антипаттерны). Если эталона нет —
   писать вопрос, не обвинение.

## Структура письма (реверс-инжиниринг письма про Guarantees)

1. **Точный скоуп**: schema.table, source, отчётная дата/период, объём (сколько строк
   проверено) — чтобы получатель не мог сказать «у меня по-другому», не проверив тот
   же срез.
2. **Правило, которое нарушено**, как формула, не как «не сходится»:
   `g_reserves_kzt = g_reserves_currency × 100`.
3. **Количественный эффект**: и агрегат (49.2 млрд → 492 млн), и системность
   («отклонений нет, за исключением округления» — т.е. не выборочный шум).
4. **Независимая перекрёстная проверка ВТОРЫМ методом/полем.** Guarantees: сумма
   ÷100 сходится не только по абсолютной величине, но и по проценту
   (`g_ifrs_reserves_percentage`) на конкретных БИН — два независимых пути к одному
   выводу. Это то, что превращает `hypothesis` в `proven`/`verified` на холсте.
5. **Явно, чего НЕ знаем.** `payments`-скрипт прямо пишет: без transaction ID нельзя
   отличить техническое дублирование от повторных реальных операций — это не
   слабость находки, это честная граница того, что доказуемо средствами целевой
   таблицы.
6. **Конкретный список просьб**, не «разберитесь»: проверить алгоритм/маппинг,
   подтвердить источник множителя, устранить дефект, перезагрузить данные, объяснить
   семантику поля.
7. **Ограничение на использование до исправления.** «Поле не может использоваться для
   расчёта провизий... до устранения дефекта» — явно, а не подразумевается.
8. **Воспроизводимый SQL приложен** — получатель не обязан верить на слово.

## Инвентарь: репортили vs накопили и не отправили (по состоянию на 04.08.2026)

| # | Находка | Источник | Статус эскалации |
|---|---|---|---|
| 1 | `Guarantees.g_reserves_kzt` ×100 | Sailau → Kadirzhan, письмо 04.08 13:33, эскалация Akberdiyev 16:15 | **Отправлено, в работе** — ничего не требуется с нашей стороны |
| 2 | `loans` дубли gid по всем источникам (S01=70/S02=24/S03=1/S17=0) + эффект на `repayment_schedule` | Наш L1.1 + независимо Sailau, числа совпали | **Отправлено** — письмо №19, 04.08 17:51, Sailau → Kadirzhan, cc Miras/Diana/Damir/Sabila/Yerlan |
| 3 | `payments` точные дубли (94 069 избыточных строк, до 2.87 млрд ₸ сценарно) | Sailau, evidence-скрипт есть | **Отправлено** — письмо №18, 04.08 15:42 (тема упоминает только `payments_wiring`, тело покрывает обе таблицы), те же получатели |
| 4 | `payments_wiring` S02: формула `total` (254 021 строк, 384.46 млн ₸) + дубли (18 269 групп) | Sailau, evidence-скрипт с regression-контролем | **Отправлено** — то же письмо №18 |
| 5 | L0.2: 4 схемных type mismatch (borrower_id, collateral_id, loan_number, loan_id×repayment_schedule) | Наш L0 (`is_gate=1`, блокирует TRUST_LEVEL всех 13 слоёв) | Задокументировано построчно по всему скрипту, **не отправлено** — черновик письма ниже |
| 6 | S03 pledges по корректному ключу: 94 732 договора без залога (было 49 по DWH-15, ключ был неверный) | Наш L9.2, закрывает `FINDINGS.md` §9.5 п.2 | Получатели письма от 17.07 (старое число «49») не уведомлены о коррекции — **не отправлено**, черновик ниже |
| 7 | L12.3 restructuring↔loans по gid (сегодняшний фикс) | Наш скрипт, только что исправлен | Требует повторного прогона на живых данных перед любым письмом — **не готово к отправке** |
| 8 | Потеря 90+ при переходе (S02 135.8М + S03 957.5М + S17 272.2М ≈ 1.365 млрд ₸) | `FINDINGS.md` §6 (S17-часть, письмо №4) + наш L8.5/L8.5b (консолидация всех 3 источников) | S17-часть отправлена (письмо №4, 16.07); **консолидация по S02/S03 не отправлена** — черновик ниже |
| 9 | `FINDINGS.md` §7 — 15 открытых вопросов, явно помечены «только человек / автор mart» | Накоплено за всю сессию | Большинство пунктов уже покрыты письмами №1/2/4/6/7/8 по отдельности; пп. 6/7/8/10/11/12/15 **не заданы явно ни в одном письме** — см. «Ещё не заданные пункты» ниже |
| 10 | S03 provisions +30.6 млрд (MATCHED) | `FINDINGS.md` §4, самая крупная находка проекта | Валентность сознательно НЕ присвоена (нет эталона) — **правильно, что не отправлено как обвинение**; но сам факт расхождения тоже нигде не эскалирован формально |

Пункты 5, 6, 8 — черновики готовы ниже, по шаблону выше (Кому/Копия как в письмах №17–19).
Пункт 7 — жди повторный прогон. Пункт 9 (остаток) — список без полных писем, см. ниже.
Пункт 10 — жди эталон (бух/IFRS 9), не пиши как дефект, пока валентность не на чём присвоить.

## Черновики писем (готовы к отправке после вашей правки)

### Черновик 1 — L0.2: несовместимость типов ключей (4 связки)

**Кому:** Ақбердиев Қадіржан
**Копия:** Закарьянов Мирас, Умарова Диана, Джакипбеков Дамир, Чакенова Сабила, Әріпбай Ерлан
**Тема:** Несовместимость типов ключей в Dictionaries.risk_analytics — 4 связки блокируют
доверие ко всем последующим слоям проверки

В рамках послойной проверки `Dictionaries.risk_analytics` (`INFORMATION_SCHEMA`, срез
01.07.2026, read-only) выявлены 4 связки ключей с несовместимыми типами данных:

1. `loans.l_borrower_id` (varchar) ↔ `borrower.b_borrower_id` (bigint)
2. `loans.l_collateral_id` (varchar) ↔ `pledges.c_collateral_id` (float)
3. `loans.l_loan_number` (varchar) ↔ `loan_account.la_dog_num` (nvarchar)
4. `loans.l_loan_id` (nvarchar) ↔ `repayment_schedule.rs_loan_id` (bigint)

**Эффект:** JOIN по этим парам требует неявного приведения типов — это (а) делает JOIN
non-sargable (практически подтверждено: коррелированный JOIN по паре 4 вызвал зависание
проверки на 2+ часа на прогоне 04.08.2026 до перехода на явный `CONVERT`), и (б) меняет
семантику сравнения — varchar `'007'` физически не равен bigint `7`, даже если по смыслу это
один номер.

**Просим:** подтвердить, преднамеренно расхождение типов или это дефект миграции; дать
каноническое правило сравнения для каждой из 4 связок (сырое или приведённое); рассмотреть
унификацию типов на уровне схемы, если расхождение непреднамеренно.

**До ответа:** все проверки по этим связкам помечены в нашем отчёте `TRUST_LEVEL=ДОВЕРЯТЬ
НЕЛЬЗЯ` — это архитектурный гейт на все 13 нижестоящих слоёв проверки, не 4 отдельных чека.

Приложен воспроизводящий SQL (`L0.2`, `sql/risk_dwh_layered_check.sql`, без PII).

---

### Черновик 2 — Уточнение к письму от 17.07: залоги S03 (49 → 94 732)

**Кому:** Ақбердиев Қадіржан (и/или Sailau, если находка 17.07 — его собственная)
**Копия:** Закарьянов Мирас, Умарова Диана, Джакипбеков Дамир, Чакенова Сабила, Әріпбай Ерлан
**Тема:** Уточнение к письму от 17.07.2026 — залоги S03: было 49, при корректном ключе 94 732

Уточнение к находке из письма от 17.07.2026 («Результаты проверки справочников заёмщиков и
залогов на 01.07.2026», п.3): «49 активных договоров S03 с заполненным `l_collateral_id` без
записи в `pledges`, суммарный баланс 367 700 497,14 ₸».

При повторной проверке обнаружено: тот прогон присоединял `pledges` к `loan_account` по
`la_loan_id = t.loan_id` — ключу, не входящему в подтверждённый канонический набор
(NEW-internal связи строятся по паре source+gid, как `loan_account↔loans`:
`la_gid=l_gid AND la_source=l_source`). При пересчёте по корректному ключу
(`pledges.c_source + c_loan_gid = loans_active.l_source + l_gid`) число возрастает с **49 до
94 732** договоров S03.

**Просим:** число 49 не использовать как основание для дальнейшего анализа/отчётности;
уточнённая сумма баланса будет предоставлена отдельно после согласования; считать это письмо
заменой, не дополнением к находке от 17.07.

Приложен воспроизводящий SQL (`L9.2`, `sql/risk_dwh_layered_check.sql`).

---

### Черновик 3 — Потеря 90+ при переходе: консолидация S02/S03/S17

**Кому:** Ақбердиев Қадіржан
**Копия:** Закарьянов Мирас, Умарова Диана, Джакипбеков Дамир, Чакенова Сабила, Әріпбай Ерлан
**Тема:** Потеря признака 90+ при переходе в новую ветку — консолидация по S02/S03/S17,
совокупно ~1,365 млрд ₸

Консолидация ранее направленной по S17 находки (письмо от 16.07.2026) с данными по S02/S03:
договоры, где старая ветка показывает признак 90+, а новая — нет (включая NULL).

| Источник | Договоров | Сумма, ₸ |
|---|---|---|
| S17 | 932 (247 → NULL, 685 → ≤90) | 273 000 212,61 |
| S02 | 1 542 | 135 810 925,60 |
| S03 | 187 | 957 547 690,51 |
| **Итого** | **2 661** | **1 365 358 828,72** |

**Регуляторная значимость:** прямое занижение NPL/Stage 3 при переключении на новую ветку без
исправления DPD.

**Просим:** подтвердить причину потери признака по S02/S03 (тот же механизм, что у S17 — смена
расчёта DPD между загрузками, — или иная причина); предоставить план исправления до перехода
новой ветки в регуляторную отчётность.

Приложен воспроизводящий SQL (`L8.5`/`L8.5b`, `sql/risk_dwh_layered_check.sql`).

## Ещё не заданные пункты FINDINGS.md §7 (короткий список, не полные письма)

Остаток открытых вопросов реестра, ни разу не заданный явно ни в одном из 19 писем:

- **п.6** S02 product-разметка — отброшена в миграции или берётся из справочника по gid?
- **п.7** Владелец mart-модели — организационный вопрос, не данные; решать не письмом-находкой.
- **п.8** Источник текущего провизионного отчёта — откуда 8,5 млрд по расторгнутым (двойной учёт?).
- **п.10** Resolution-пайплайн (`writeoff`/`collections`/`Offbalance`) — план покрытия S02/S17?
- **п.11** `writeoff` grain — 84 строки на весь S01+S03: «1 договор» или «1 кейс списания»?
- **п.12** S03 18771-валентность — атрибуция «1428=модель, 18771=ETL» не подтверждена (85%
  дельты внутри MATCHED, не population); наша находка, не Sailau — черновик письма не готов.
- **п.15** S17 ONLY_NEW 18771 (8,26 млн) сидит на 18 договорах из 10 979 — что в этих 18?

Каждый — кандидат на короткое отдельное письмо (2–4 абзаца по шаблону выше), если приоритет
подтверждён.

## Стратегии найти больше (если инвентарь исчерпан)

1. **Искать тот же класс бага в других местах.** ×100-масштабный дефект (Guarantees)
   почти никогда не бывает изолированным — стоит систематически сверить ВСЕ парные
   currency/percentage-поля схемы (не только `g_reserves_*`) на ту же кратность.
   Аналогично: exact-duplicate детектор (метод из `payments`/`payments_wiring`) ни разу
   не применялся к `collections`, `offbalance`, `bankrupt`, `credit_lines`, `ratings`,
   `interest_rates` — ни одна из этих шести таблиц не имеет своего дубль-чека.
2. **Механически проверить весь репозиторий на loan_id-vs-gid антипаттерн.** Мы
   поймали его дважды (pledges, restructuring_v2) за счёт того, что оба раза кто-то
   читал SQL построчно. Нужен систематический grep всех `JOIN`/`NOT EXISTS` на
   `loan_id`/`la_loan_id`/`l_loan_number` там, где рядом есть `gid`-колонка с тем же
   назначением — включая скрипты Сайлау, не только наш.
3. **Прогнать оставшуюся vacant-column программу.** `SAILAU_METHOD.md` уже
   спроектировал масштабирование на 158 непроверенных колонок (Batch 2+,
   `offbalance` — Tier A, кандидат №1) — SQL готов
   (`Risk_DWH_Vacant_Column_Retest_Pack_20260803.sql`), не прогнан.
4. **Reconciling-запросы из `FINDINGS.md` §9.5, ещё не выполненные**: S02
   `ONLY_OLD` (124 964) против DWH-04's «выпало из `loan_account`» (159 603) по
   общему `l_gid`; «68 vs 44» (наши vs DWH-02 SMART_CARD) — оба дёшевы, оба
   потенциально закрывают открытый вопрос данными, не оценкой.
5. **DWH-13/14/16 — независимая проверка ключевым методом по покрытию**, ещё не
   сделана (`FINDINGS.md` §9.2/§9.5 п.5) — по профилю Miras, до подписи под чужими
   числами в общем письме.
