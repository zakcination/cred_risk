# 100 сценариев аналитика для тестирования DWH `Dictionaries.risk_analytics`

Набор реальных запросов, какие приходят в аналитику розничного/корпоративного риска,
разложенный по трём уровням сложности. Назначение — **тестирование DWH**, поэтому у
каждого сценария указано не только «что посчитать», но и **что он проверяет / на какой
ловушке ломается**.

## Как этим пользоваться

Колонка «Что тестирует / ловушка» — главная. Сценарий, который **невозможно** выполнить
корректно из-за известного дефекта, ценнее для теста, чем сценарий, который проходит
гладко: он показывает границу пригодности марта. Ловушки взяты не из головы, а из
`FINDINGS.md` (§1–§9, D4.1/D4.2) и живых прогонов `CASE_RUN_*` этой сессии.

**Критерий прохождения** для каждого сценария — не «запрос вернул строки», а:
1. использован канонический ключ (`*_gid = l_gid AND *_source = l_source`), не имя-похожее поле;
2. NULL отделён от нуля и от «записи нет»;
3. число воспроизводится вторым, независимо написанным запросом;
4. если источники ведут себя по-разному — это видно в разрезе, а не усреднено.

## Библиотека ловушек (T1–T25) — ссылки из таблиц ниже

| Код | Ловушка | Источник |
|---|---|---|
| **T1** | `l_collateral_id` → `pledges` **ОПРОВЕРГНУТ** как FK. Единственный путь: `c_source+c_loan_gid = l_source+l_gid` | bi_canvas `d8`, DWH-16 |
| **T2** | `la_loan_id` не входит в подтверждённые ключи; для S03 даёт **0 совпадений** | FINDINGS §1, DWH-15 |
| **T3** | Типовой разнобой: `l_borrower_id` varchar ↔ `b_borrower_id` bigint | L0.2 (гейт) |
| **T4** | Типовой разнобой: `l_loan_id` nvarchar ↔ `rs_loan_id` bigint. JOIN без CAST не матчит НИЧЕГО (ложные 586 717) | L0.2, L11.1 |
| **T5** | `c_collateral_id` имеет тип **`float`** — идентификатор с плавающей точкой | Прогон №1, 31.07 |
| **T6** | **S01: `c_collateral_id` КОЛЛИЗИРУЕТ** между разными физическими объектами (100% из 265 пар — разный `c_bpm_object_id`) | FINDINGS D4.1 |
| **T7** | `c_bpm_object_id` заполнен 100% у S01/S17 и **0% у S03** — для S03 перепроверить идентичность объекта нечем | CASE_RUN_013 A1a |
| **T8** | **Историзации нет.** `risk_analytics` перезаписывается целиком; в `loans` ровно одна `l_report_date`. Любой запрос «как было в прошлом месяце» невыполним | FINDINGS §9.7 |
| **T9** | S02: продуктовые поля (`l_product_type` и др.) **NULL на всех** строках | bi_canvas `d3` |
| **T10** | S02: залогов нет вообще — 100% `NO_COLLATERAL`. «0 покрытия» здесь означает продукт, а не дефект | CASE_RUN_013 B1 |
| **T11** | S17: `l_collateral_id` 100% NULL, но залоги **есть** и находятся по gid. Фильтр по `l_collateral_id IS NOT NULL` потеряет весь S17 | CASE_RUN_011 B1 |
| **T12** | `l_limit` NULL на всех 77 457 протестированных — мэппинг из старого `limit` опровергнут | bi_canvas `d6` |
| **T13** | `dpd = days_past_due − 1` (off-by-one), подтверждено | FINDINGS §3, `f5` |
| **T14** | S17: `days_past_due_interest` **100% NULL** на всех 79 617 matched — измерение потеряно | Прогон №1 |
| **T15** | `total_balance_debt` считается **по разной формуле** между источниками (S17 без 1818/1838, S03 включает) | L6.0 |
| **T16** | У `borrower` **нет gid-колонки** — джойн только по `borrower_id`, с CAST | L4.1 |
| **T17** | Префикс gid = source: 10=S01, 11=S02, 12=S03, 27=S17 (сумма сходится точно) | CASE_RUN_010 R04 |
| **T18** | `l_gid` глобально уникален, но `l_loan_number` даёт **9 659 коллизий** между источниками. Группировка по номеру договора без source — смешивает клиентов | FINDINGS §1 |
| **T19** | `delinquency_bucket` формируется **независимо** от DPD-полей (S17) — не выводить одно из другого | Прогон №1 |
| **T20** | `writeoff` — 84 строки на весь S01+S03. Grain («1 договор» или «1 кейс») не подтверждён | FINDINGS §7 п.11 |
| **T21** | `ratings.r_deal_gid` — **только префикс 10**, т.е. рейтинги есть лишь у одного источника | CASE_RUN_010 R03 |
| **T22** | `Guarantees.g_reserves_kzt` = `g_reserves_currency` × 100 (дефект ×100, в эскалации) | Письмо Сайлау 04.08 |
| **T23** | `payments`/`payments_wiring`: точные дубли (94 069 избыточных строк); `total` S02 не сходится с компонентами | Письмо №18 |
| **T24** | `repayment_schedule` S01: физические дубли — 11 групп / 443 строки / 385 005 157,47 ₸ | Письмо №20 |
| **T25** | `l_rate` — тип **varchar(255)**, не число. Любая арифметика по ставке требует приведения и проверки на нечисловые значения | Схема |

---

# Уровень 1 — ЛЁГКИЙ (одна таблица, фильтр/группировка)

Проверяет: населённость полей, домены значений, типы, NULL-семантику.

| # | Запрос аналитика | Таблица | Что тестирует / ловушка |
|---|---|---|---|
| E01 | Количество активных договоров в разрезе источника | `loans_active` | Базовый домен `source` (S01/S02/S03/S17 — один словарь) |
| E02 | Распределение договоров по продукту (`l_product_type`) | `loans` | **T9** — по S02 вернётся 100% NULL, это не пустой портфель |
| E03 | Портфель по филиалам (`l_branch_code`/`l_branch_name`) | `loans` | Согласованность кода и названия филиала: один код ↔ одно название? |
| E04 | Разбивка по валюте договора | `loans` | Домен `l_currency`: коды ISO или свободный текст? |
| E05 | Распределение по сроку (`l_initial_term_months`) бакетами 0–12/12–36/36–60/60+ | `loans` | Отрицательные/нулевые/аномально большие сроки |
| E06 | Распределение суммы кредита (`l_loan_amount`) по децилям | `loans` | Нули и отрицательные суммы; тип `float` |
| E07 | Средняя/медианная ставка по продукту | `loans` | **T25** — `l_rate` varchar; сначала аудит нечисловых значений |
| E08 | Договоры, открытые за последние 12 месяцев, помесячно | `loans` | `l_loan_open_date` vs `l_funding_date` — что считать «выдачей» |
| E09 | Договоры без даты фактического закрытия (`l_actual_closure_date IS NULL`) | `loans` | Пересечение с `loans_active` — DWH-03 сообщал 3 909 таких вне периметра |
| E10 | Договоры с датой погашения в прошлом, но без закрытия | `loans` | Просроченные по сроку, но формально живые |
| E11 | Заполненность `l_limit` и `l_unutilized_limit` | `loans` | **T12** — ожидается 100% NULL по `l_limit` |
| E12 | Распределение статусов (`l_loan_status`) | `loans` | Домен статусов; сравнить со списком в `loans_active` |
| E13 | Клиенты по регионам | `borrower` | Домен `b_region` (varchar 1000 — свободный текст?) |
| E14 | Клиенты по типу (`b_borrower_type`) — ФЛ/ЮЛ/ИП | `borrower` | Согласованность с `b_individual_entrepreneur_flag` |
| E15 | Возрастные бакеты клиентов | `borrower` | `b_date_of_birth`: даты из будущего, 1900-01-01 как «нет данных» |
| E16 | Клиенты с признаком банкротства | `borrower` | `b_bankruptcy_flag` varchar(-1) — домен значений (Y/N? 1/0? текст?) |
| E17 | Клиенты с POCI-признаком | `borrower` | `b_poci_flag` + непустая `b_poci_assignment_date` — согласованы? |
| E18 | Распределение дохода клиентов (`b_monthly_income`) | `borrower` | `numeric(10,0)` — переполнение на крупных ЮЛ? нули = «нет данных»? |
| E19 | Нерезиденты в портфеле | `borrower` | `b_non_resident_flag` varchar(2) |
| E20 | Умершие клиенты (`b_date_of_death` заполнена) | `borrower` | Кросс-проверка с активными договорами (см. M28) |
| E21 | Клиенты с рейтингом и датой рейтинга старше 12 мес. | `borrower` | Устаревание `b_client_rating_date` |
| E22 | Суммарный остаток по источникам | `loan_account` | **T15** — `total_balance_debt` не сопоставим между источниками напрямую |
| E23 | Распределение DPD бакетами 0/1-30/31-60/61-90/90+ | `loan_account` | **T13** off-by-one; **T14** NULL ≠ 0 |
| E24 | Распределение `delinquency_bucket` | `loan_account` | **T19** — не выводить из DPD; сверить независимо |
| E25 | Договоры с ненулевым счётом 1401 / 1428 / 18771 | `loan_account` | Семантика счетов НБК; 18771 — открытый вопрос реестра |
| E26 | Заполненность всех DPD-полей по источникам | `loan_account` | **T14** — целое измерение отсутствует у S17 |
| E27 | Типы залогов и их доли | `pledges` | Домен `c_collateral_type` |
| E28 | ТОП-20 марок авто в залоге | `pledges` | `c_car_brand` — свободный текст, нужна нормализация |
| E29 | Залоги с оценкой старше 24 месяцев | `pledges` | `last_appraisal_date` — регуляторное требование переоценки |
| E30 | Распределение залоговой стоимости | `pledges` | Нули, отрицательные, аномалии >10× (539 S01 / 42 S03 известны) |
| E31 | Залоги без оценщика (`nok_appraiser_name` пуст) | `pledges` | Полнота НОК-блока |
| E32 | Количество платежей и объём помесячно | `payments` | **T23** — дубли завышают объём |
| E33 | Реструктуризации помесячно | `restructuring_v2` | `report_date` vs `restructuring_date` |
| E34 | Реструктуризации с отменой (`canc_date` заполнена) | `restructuring_v2` | Доля отменённых — качество процесса |
| E35 | Распределение отсрочки платежа (`payment_deferral`) | `restructuring_v2` | Единица измерения: дни? месяцы? Не документировано |

---

# Уровень 2 — СРЕДНИЙ (JOIN 2–4 таблиц, группировка, оконные функции)

Проверяет: корректность ключей, кардинальность, целостность связей.

| # | Запрос аналитика | Таблицы | Что тестирует / ловушка |
|---|---|---|---|
| M01 | **Полное досье по одному клиенту**: все договоры + сумма, ставка, срок, продукт, портфель, рейтинг, залоги | `borrower`+`loans`+`loan_account`+`pledges`+`ratings` | **Флагманский сценарий.** T16 (borrower только по borrower_id + CAST), T1 (залог по gid, не по l_collateral_id), T21 (рейтинг может отсутствовать) |
| M02 | Клиенты с более чем одной реструктуризацией | `borrower`+`loans`+`restructuring_v2` | `dlcr_gid = l_gid` (проверено, 81 187/81 188); агрегация на уровне клиента, не договора |
| M03 | Договоры с более чем одним залогом | `loans_active`+`pledges` | **T6** — часть «нескольких залогов» у S01 может быть коллизией ID, а не реальной множественностью |
| M04 | Один залог под несколькими договорами (общее обеспечение) | `pledges`+`loans_active` | **T6/T7** — 99 из 956 групп S01 показывают разный `c_bpm_object_id` (не настоящее совместное обеспечение) |
| M05 | Количество договоров на клиента, ТОП-50 | `borrower`+`loans` | Сверка с `b_active_loans_count` — сходится ли счётчик в справочнике с фактом |
| M06 | Портфель и остаток в разрезе регион × продукт | `borrower`+`loans`+`loan_account` | **T9** (S02 NULL-продукт) + **T15** (несопоставимый баланс) одновременно |
| M07 | Средняя ставка по продукту, взвешенная по остатку | `loans`+`loan_account` | **T25** (varchar-ставка) + выбор базы взвешивания |
| M08 | Покрытие залогом: залоговая стоимость / сумма кредита, по источникам | `loans_active`+`pledges` | **T6** — S01 даёт 533% на агрегате (нереалистично, см. D4.2); дедуп по объекту обязателен |
| M09 | Договоры с заполненным `l_collateral_id`, но без строки в `pledges` | `loans_active`+`pledges` | **T1/T11** — исторически 49 → 94 732 при смене ключа; сейчас 18 |
| M10 | Залоги, не находящие договор | `pledges`+`loans_active`+`loans` | Разделить «договор закрыт» и «истинный orphan» (сейчас orphan = 0) |
| M11 | Клиенты с несколькими активными договорами в разных источниках | `borrower`+`loans_active` | **T17** — префикс gid подтверждает источник; **T18** — не группировать по номеру договора |
| M12 | Экспозиция по группе связанных заёмщиков | `borrower`+`loans`+`loan_account` | `b_group_affiliation` тип `text` — как парсится? Регуляторный лимит на группу |
| M13 | Договоры без графика погашения | `loans_active`+`repayment_schedule` | **T4** — без CAST даст ложные «все 586 717» |
| M14 | Договоры с графиком, но без единого платежа | `repayment_schedule`+`payments` | Ключ `payments` — только `p_CREDIT_ACCOUNT`; связь с договором не подтверждена |
| M15 | Просрочка по графику: плановый платёж vs фактический | `repayment_schedule`+`payments_wiring` | **T23/T24** — дубли с обеих сторон |
| M16 | Реструктурированные договоры: до/после ставки | `loans`+`restructuring_v2` | `new_interest_rate` float vs `l_rate` varchar — сравнение требует приведения |
| M17 | Реструктуризации с датой погашения раньше даты реструктуризации | `restructuring_v2` | 184/4 520 известны — воспроизвести |
| M18 | Договоры в реструктуризации на отчётную дату (grace-период активен) | `restructuring_v2`+`loans_active` | Трёхзначная логика: активна / не активна / **даты не заполнены** (см. safezone-план) |
| M19 | Списанные договоры и их последний известный остаток | `writeoff`+`loans`+`loan_account` | **T20** — grain `writeoff` не подтверждён, 84 строки на два источника |
| M20 | Банкроты: договоры и экспозиция | `bankrupt`+`loans`+`loan_account` | `b_dog_gid` подтверждён как то же пространство (4 208/4 208) |
| M21 | Рейтинг клиента vs рейтинг сделки — расхождения | `ratings`+`borrower` | **T21** — только один источник; `r_dog_num` vs `r_deal_gid` — какой ключ рабочий |
| M22 | Гарантии: резерв в тенге vs в валюте | `Guarantees` | **T22** — ожидается ровно ×100, дефект в эскалации |
| M23 | Договоры с гарантией и залогом одновременно | `Guarantees`+`loans`+`pledges` | `g_clnt_gid` — **другое пространство** (0% совпадений с l_gid), связать по нему нельзя |
| M24 | Доля обеспеченных договоров по продукту | `loans_active`+`pledges` | **T10** (S02 без залогов по продукту) vs реальный дефект — не перепутать |
| M25 | Договоры с DPD 90+ и их обеспечение | `loan_account`+`loans_active`+`pledges` | **T13** off-by-one при отборе 90+ |
| M26 | Портфель по сегменту и entrepreneur_category | `loans`+`borrower` | Согласованность `l_segment`, `l_entrepreneur_category`, `b_borrower_type` |
| M27 | Клиенты-ИП: договоры как ФЛ и как ИП | `borrower`+`loans` | `b_individual_entrepreneur_flag` + ИИН/БИН — один человек, две роли |
| M28 | Активные договоры умерших клиентов | `borrower`+`loans_active`+`loan_account` | Операционный риск; `b_date_of_death` |
| M29 | Активные договоры клиентов в местах лишения свободы | `borrower`+`loans_active` | `b_date_of_imprisonment`, `b_status_change_date_prison` |
| M30 | Сверка `b_active_loans_count` с фактическим счётом | `borrower`+`loans_active` | Прямой тест внутренней согласованности справочника |
| M31 | Договоры, где `l_loan_number` не уникален | `loans` | **T18** — 9 659 коллизий; разложить: внутри источника или между |
| M32 | Один клиент — несколько borrower_id | `borrower` | Дубли клиента по ИИН/БИН (**PII: только агрегат**) |
| M33 | Залоги: рыночная vs залоговая стоимость, расхождение >10× | `pledges` | Известно: 539 S01 / 42 S03 — воспроизвести |
| M34 | Авто-залоги старше 15 лет (`c_car_year`) | `pledges` | `c_car_year` — nvarchar; нечисловые значения |
| M35 | Договоры с несколькими записями в `loan_account` на одну дату | `loan_account` | Grain-тест; S02 8 дублей известны |
| M36 | Средний срок до первого платежа | `loans` | `l_funding_date` → `l_first_repayment_date`; отрицательные интервалы |
| M37 | Договоры с досрочным закрытием (факт < план) | `loans` | `l_actual_closure_date` vs `l_scheduled_closure_date` |
| M38 | Провизии vs остаток по источникам | `loan_account` | Формула провизий S03 верна, но сходится на 21,23% строк — не смешивать |
| M39 | Валютные договоры: переоценка по курсу | `loan_account`+`loans` | `currency_exchange_rate` — на какую дату? |
| M40 | Портфель по финансовому консультанту | `loans` | `l_financial_consultant` — качество продаж по сотруднику |

---

# Уровень 3 — СЛОЖНЫЙ (сильная логика, временные ряды, аналитика)

Проверяет: пригодность DWH для риск-моделирования и регуляторной отчётности.
**Общая ловушка для всего уровня — T8: истории нет.** Любой сценарий «динамика во
времени» требует либо собственного архива снимков, либо признания, что он невыполним
на текущем DWH. Это **главный вывод уровня 3**, а не помеха тесту.

| # | Запрос аналитика | Таблицы | Что тестирует / ловушка |
|---|---|---|---|
| H01 | **Vintage-анализ**: кривые дефолтности по когортам выдачи (месяц выдачи × месяцев на книге) | `loans`+`loan_account` | **T8** — нужен ряд снимков. На одном срезе невыполнимо |
| H02 | **Roll-rate матрица** переходов между бакетами просрочки, месяц к месяцу | `loan_account` | **T8** + **T19** (бакет независим от DPD) |
| H03 | **Cure-rate**: доля вышедших из 90+ без реструктуризации | `loan_account`+`restructuring_v2` | Прямая связь с задачей Дамира (`stage3_safezone_plan.md`) |
| H04 | **Re-default**: повторный дефолт после «излечения», окно 3/6/9 мес. | `loan_account`+`restructuring_v2` | Цензурирование: проданные/списанные ≠ «выжил» (см. safezone-план) |
| H05 | Эффективность реструктуризации: DPD до vs через 6/12 мес. после | `restructuring_v2`+`loan_account` | `days_past_due_at_restructuring` есть в таблице — сверить с `loan_account` на ту же дату |
| H06 | **LTV-распределение** и доля кредитов с LTV > 100% | `loans_active`+`pledges`+`loan_account` | **T6/T7** — LTV по S01 недостоверен до устранения коллизии (D4.2) |
| H07 | Концентрация: ТОП-20 заёмщиков, доля в портфеле, регуляторный лимит | `borrower`+`loans`+`loan_account` | **T15** — суммировать баланс между источниками некорректно без единой формулы |
| H08 | Концентрация по типу залога: что будет при падении цен на 30% | `pledges`+`loan_account` | Стресс-тест; **T6** искажает базу |
| H09 | Концентрация по региону × продукт × срок | `borrower`+`loans`+`loan_account` | **T9** — S02 выпадает из продуктового среза |
| H10 | **Миграция рейтингов** клиента за период | `ratings` | **T8** + **T21** (только один источник) |
| H11 | Платёжная дисциплина: доля платежей в срок / с опозданием N дней, по продуктам | `repayment_schedule`+`payments_wiring`+`loans` | **T23/T24** дубли; **T4** ключ графика |
| H12 | Досрочное погашение: частота, размер, влияние на процентный доход | `repayment_schedule`+`payments` | Отличить досрочное от планового — нужна логика сопоставления сумм |
| H13 | **Прогноз денежного потока** по графику на 12 мес. вперёд | `repayment_schedule`+`loans_active` | Договоры без графика (M13) молча выпадут из прогноза |
| H14 | Разрыв между графиком и фактом на уровне портфеля | `repayment_schedule`+`payments_wiring` | Net vs gross — встречные отклонения гасят друг друга |
| H15 | **Достаточность обеспечения** под 90+: непокрытая экспозиция | `loan_account`+`pledges`+`loans_active` | **T6** + **T13**; регуляторно значимая цифра |
| H16 | Резервы: фактические vs расчётные от стоимости залога | `loan_account`+`pledges` | **T6** — до устранения коллизии по S01 расчёт невозможен |
| H17 | Портфель под риском при дефолте группы связанных лиц | `borrower`+`loans`+`loan_account` | `b_group_affiliation` (`text`) — парсинг не документирован |
| H18 | **Early-warning**: рост DPD + реструктуризация + падение оценки залога одновременно | `loan_account`+`restructuring_v2`+`pledges` | Трёхсторонняя связь; **T8** для тренда |
| H19 | Сравнение риск-профиля каналов выдачи (консультант/филиал) | `loans`+`loan_account`+`borrower` | Смещение выборки: разные каналы = разные продукты, не только качество |
| H20 | Влияние отсрочки (`payment_deferral`) на итоговый исход договора | `restructuring_v2`+`loan_account` | Единица `payment_deferral` не документирована |
| H21 | **Полный резолюшн-пайплайн**: активный → 90+ → collections → writeoff → offbalance | `loan_account`+`collections`+`writeoff`+`offbalance` | **T20**; `collections`/`offbalance` — вообще не проверены |
| H22 | Recovery rate по списанным договорам | `writeoff`+`payments` | Поступления после списания — есть ли они в `payments` |
| H23 | Сверка витрины `brm_all_data` с исходными таблицами | `brm_all_data`+все | Видно 3 колонки — что преджойнено и на какой грануляции (FINDINGS §11 п.7) |
| H24 | **Кросс-источниковая согласованность**: один клиент, разные источники, разные атрибуты | `borrower`+`loans` (все source) | **T17/T18**; один ИИН — разные `b_region`/`b_borrower_type`? |
| H25 | Полнота DWH vs старая ветка `CL_PORTFOLIO`: что потерялось при миграции | `risk_analytics`+`CL_PORTFOLIO` | Cross-db работает только на одном инстансе; потеря 90+ ~1,365 млрд ₸ известна |

---

## Сводка покрытия

| Уровень | Кол-во | Таблиц задействовано | Основной фокус |
|---|---|---|---|
| Лёгкий | 35 | 9 | населённость, домены, типы, NULL |
| Средний | 40 | 14 | ключи, кардинальность, целостность |
| Сложный | 25 | 18+ | пригодность для риск-моделей и отчётности |
| **Итого** | **100** | | |

## Что этот набор НЕ покрывает (честная граница)

- **`collections`, `offbalance`, `credit_lines`, `interest_rates`** — задействованы
  только в H21; ни одна не имеет собственного дубль-чека и не проверена по покрытию.
  Отдельная программа (см. `DEFECT_REPORT_GUIDE.md`, стратегия 1).
- **S02 глубоко** — сценарии его затрагивают, но самый рисковый источник (UNION трёх
  карточных таблиц через миграционный обрыв фев-2026) требует собственного набора,
  начиная с шагов 0–4 спирали (`CLAUDE.md` → «Следующий шаг»).
- **Динамика во времени** — H01/H02/H10/H18 сформулированы как аналитик их задаст, но
  на текущем DWH невыполнимы (**T8**). Это результат теста, а не пробел набора.
- **PII-сценарии** (поиск по ИИН/ФИО) сознательно не включены: правило проекта —
  только агрегаты (`CLAUDE.md`).
