# Контур `portfolio_report` — сегментный отчёт по кредитному портфелю

Заведён 23.09.2026. Ежемесячный управленческий отчёт по портфелю в разрезе **сегментации**,
а не продуктов. Единственный источник — **`Dictionaries.risk_analytics`**.

## Почему витрина, а не `CL_PORTFOLIO`

`AQR20xx_B1A/B1B/B3C_*` — замороженные регуляторные выгрузки циклов, **не месячные**.
Это записано в `sql/stage3_safezone_discovery.sql` как прямое предупреждение. Для
повторяющегося отчёта они непригодны по построению: снимок цикла — не поток.

---

**Обновлено 24.09.2026 (12):** прогнан `RPT_H`, Р-8 разобрана по четырём объяснениям
(см. реестр). Главное: с апреля 2026 доля выданных карт с лимитом упала с 13 % до 1,8 %
и с возрастом не догоняет — ступенька, а не лаг снимка. И до апреля 71–93 % «выдач»
S02 — карты без лимита, которыми почти не пользуются. Правило «ведущего множителя»,
записанное до прогона, оказалось дефектным (концы на 56 и 3 картах) — признано явно.
В `loans` найдена колонка `l_unutilized_limit`, которой нет в реестре полей, — паспорт
отдельным PR в `dict_registry`. Развилка К-1 — счёт выдач карт — за автором.

**Обновлено 24.09.2026 (11):** по Р-8 написан `sql/RPT_H_card_limits.sql`. Добавлены два
объяснения к двум прежним: обнуление лимита при закрытии или блокировке и снижение
лимитов. Самопроверка — повтор чисел Power BI: 760 карт и 78,6 млн ₸ в 09.2025.

**Обновлено 24.09.2026 (10):** страница «1 · По источникам»: разрез по источникам
сверен с фактами на 2026-08 (выдачи, количество, погашения — до единицы). Заведена **Р-8**:
средний лимит на новую карту S02 упал со 103 тыс. ₸ до 1,2 тыс. — снимок или смена продукта.
Меры «Объём выдач» и «Лимиты по картам» дают пусто, а не 0,0, где показатель неприменим.

**Обновлено 24.09.2026 (9):** заведён `VINTAGE_PLAN.md` — план кривых поколений выдач.
Без них отчёт сравнивает продукты разного возраста: покрытие нового продукта Fenix 1,9 %
против 28,3 % у старого POS — это возраст, а не качество. План по §6; развилки В-1…В-4
за автором; скрипт — после согласования.

**Обновлено 24.09.2026 (8):** комплект сборки Power BI — `powerbi/BUILD.md` (11 шагов,
проверка на каждом, семь контрольных клеток с известным ответом) и `powerbi/PBI_model.dax`
(весь DAX одним файлом). Исправлены два места прежней редакции `POWERBI.md`, на которых
сборка бы споткнулась: сортировка уровней `dim_product` одним столбцом и параметр
полей в строках для мер — заменён таблицей показателей и мерой-переключателем.

**Обновлено 24.09.2026 (7):** виток 2 прогнан целиком — оба факта с ключом продукта.

- **Контроль `fact_flow` сошёлся:** S17 12.2025 — 35 679 закрытий, 30 764 с просрочкой,
  как в `RPT_G`. «Полностью погашено» у старого POS за декабрь — 4 874.
- **Р-6 ослаблена.** По продуктам перенос `1428 → 1484` не выходит: у новых авто `1484`
  больше падения провизий на 7,5 млрд, у остальных — меньше. `1485` на 97,8 % сидит
  на новых автокредитах, `1484` — на 76,5 % при их доле в остатке 65,2 %. Итоговое
  совпадение по S03 было обманчивым.
- **Р-7 (новая):** новый продукт Fenix по профилю — автокредиты; выдачи S03 NewCar
  и его вместе держались ровно до марта 2026. С апреля автокредитование сократилось
  в обеих системах; POS в Fenix, наоборот, возобновлён.
- Списание — путь необеспеченных: из 53 800 договоров пачки 12.2025 на авто — 138.
- **Порядок слияния.** PR #150 был слит до того, как в него дошёл коммит с Р-4 и Р-5;
  коммит перенесён в этот PR без изменений. Прогоны 24.09.2026 шли на его версиях SQL.

**Обновлено 24.09.2026 (6):** прогнаны `RPT_G` и виток 2.

- **Р-4 подтверждена.** У S17 из 35 679 закрытых в декабре 2025 на начало месяца
  были просрочены 86,2 % (в сентябре — ноябре 7,9–10,2 %, в январе — феврале 2,6–3,4 %),
  55 % закрыто одним днём. Выбытие портфелем под статусом «Закрыт», не погашение.
  «Полностью погашено» переопределено: без договоров, просроченных на начало месяца.
- **Р-5 закрыта — перенос, а не высвобождение.** На S03 в июне 2026 появились с нуля
  счета 1484 (+29,6 млрд) и 1485 (+29,1 млрд), одновременно 1428, 1860 и 1434 упали
  сверх обычного сокращения. Формула `1428 + 1845 + 18771` с июня 2026 неполна.
  Смысл 1484/1485 — открытый вопрос бухгалтерии (№ 6 ниже).
- **Контроль закрыт:** 6 284 открытых счёта S17 без договора, остаток нулевой;
  82 815 − 6 284 = 76 531 ровно.
- **Виток 2 показал структуру.** Автокредиты S03 — 571,7 млрд, 37,6 % всего остатка банка.
  КБ S01 — 486,7 млрд на 63 заёмщика. Новый продукт S17 — 87,4 % остатка Fenix, без
  продуктовых полей. Заёмщики между продуктами перекрываются на 2,2 % — сумма по
  продуктам завышала бы их число.

**Обновлено 24.09.2026 (5):** первый прогон Power BI — предпроверка, три факта, `RPT_F`.

- **Виток 0, итог банка** (открытые счета, конец месяца): остаток 1 684,3 млрд ₸ на
  пике (09.2025) → **1 521,6** (08.2026), −9,7 %. Провизии 186,0 → 112,5 млрд, покрытие
  11,0 % → **7,4 %**. Две ступени вниз по провизиям: **12.2025, −46 млрд** — объяснена
  (списание S03 пачкой, 53 800 договоров, и уход проблемного портфеля S17) и
  **06.2026, −35 млрд по S03 — не объяснена** (Р-5).
- **Контроли:** три из четырёх сошлись. Не сошлось число открытых счетов S17
  (82 815 против 76 531) — счета без договора в `loans`, которых DICT_D не видел.
  Остаток сошёлся. Подробно — `powerbi/POWERBI.md`.
- **Виток 2 готов раньше плана:** продуктовые поля S03 — ровно прежняя иерархия Excel
  (CAR / POS / DSL / CC). Факты пересобраны с ключом продукта.
- **Списания CrediLogic (`RPT_F`) — автор был прав по существу.** В шести «пустых»
  месяцах списания **были**: 88–231 договор в месяц, 772 за шесть месяцев. Не было
  **пачек** — даты размазаны по месяцу, доля самого частого дня 0,13–0,21 против
  0,67–0,97 в месяцах пачек. Устное подтверждение от 27.07.2026 («пачки не проводились»)
  устояло **в своей формулировке** и опровергнуто **в той трактовке**, на которой
  `stage3_safezone_plan.md` построил точность re-default rate: «событий не было».
  Фоновые списания идут каждый месяц, не только в эти шесть. Правка — в самом
  `stage3_safezone_plan.md`, отдельно (Б1).
- **Второй способ `RPT_F` устроен неверно, и это моя ошибка проектирования.** Он ловит
  выход счёта из «Открыт», а у большинства списанных договоров это **расторжение**,
  которое предшествует списанию на месяцы: путь «Открыт → Расторгнут → Списан».
  Поэтому в 09.2025 и 11.2025 он показывает больше, чем первый способ, а в декабрьскую
  пачку — 14 888 против 53 800. Заложенная в скрипт ветка «способы расходятся — поле
  не то» неверна: способы мерят разные события. Пригоден иначе — как вход в
  предсписательный путь, с остатком на момент выхода.
- **Отзыв собственного отзыва (PR #144).** Прежняя формулировка: «в RPT_A блок 3 сумма
  бралась по всем 345 853 счетам без фильтра по статусу… «расхождение с AQR 39 %»
  частично создано этим». Предпроверка измерила: все счета на 01.09.2026 — 1 523,5 млрд,
  только открытые — 1 521,6. **Фильтр меняет итог на 0,12 %.** Разрыв с AQR реален;
  «частично создано мной» — неверно по величине.
- **Вопрос `RPT_C` блок 4 получил ответ без отдельного прогона:** на 01.12.2025
  провизии 181,5 млрд — в 2 % от 185,2 AQR; на 01.01.2026 — 135,3, на 27 % ниже. Между
  срезами — декабрьское списание S03 и уход проблемного S17. Значит, сходимость
  с AQR по провизиям зависит от того, снята ли база AQR до декабрьского списания.
  Остаток расходится на 3–4 % на обе даты — это периметр, а не дата.

**Обновлено 24.09.2026 (4):** заведена папка **`powerbi/`** — отчёт в Power BI по
спирали: витки 0 (итог банка) и 1 (источник) готовы сейчас и разметки строк не
требуют. Модель одна на все витки, каждый следующий виток добавляет колонку измерения.
Подробно — `powerbi/POWERBI.md`.

**Отозвано в переписке, записано здесь:** я подал как факт, что у CrediLogic в 6 месяцах
из 12 списаний не было. Это **устное подтверждение от 27.07.2026** из `stage3_safezone_plan.md`,
а не данные, и автор в нём сомневается. Проверка на витрине, независимая от архива
CrediLogic, — `sql/RPT_F_writeoff_months.sql`, двумя способами.

**Обновлено 24.09.2026 (3):** прогнаны RPT_D и RPT_E.

- **Столбцы:** четыре развилки закрыты измерением — **14 метрик из 21 считаются**.
  Решающий тест — правило «досрочно» на эталоне S03: `l_scheduled_closure_date`
  срабатывает на `Z` в 99,98 %, на `C` в 3,3 %. Подробно — `METRICS.md`.
- **Строки:** готовой разметки в витрине нет. `l_segment` — бизнес-линии банка
  (РБ/МБ/КБ/ПБ), и только на S01. Разметка выводится **деревом решений** по
  решению автора — план в **`SEGMENT_TREE_PLAN.md`**, разведка — `TREE_0`.
- **S03: выдачи прекращены с апреля 2026** — со слов автора; на данных виден только
  обрыв ряда (5 392 → 523 → 13). Гипотеза Р-3 закрыта ответом автора, а не данными.
- **Поправка:** ниже в разделе «Отбор действующего договора» стояло «занижает портфель
  примерно на 32 900 договоров неопределённого статуса». Из `dict_registry/status_map.csv`
  эта цифра не воспроизводится: неопределённых было **28 542**, после перевода S17 `Т`
  в «закрыт» (DICT_D, PR #149) — **4 756**. Исправлено там же.

**Обновлено 24.09.2026 (2):** заведена **`REPORT_STRUCTURE.md`** — форма отчёта:
что строка, что столбец, в каком порядке разделы, как собирается месяц.
До неё контур описывал метрики и поля, но не сам отчёт.

**Главный блокер оказался не там, где велась работа.** Метрики — это столбцы,
и десять из двадцати одной готовы. **Строк нет ни одной:** сегментация НСТ
построена каскадом на полях `CL_PORTFOLIO` (`debtor_type_n`, `loan_obj_n`,
`ent_type_n`), которых в витрине нет. Кандидатов на перенос каскада семь,
и **седьмой — колонка `l_segment` в самой `loans`**, которую ни один прогон
ни разу не смотрел. Если в ней готовая сегментация, каскад не переносится,
а берётся. Разрешается `RPT_E`.

**Обновлено 24.09.2026:** прогон RPT_D упал на `Invalid column name 'l_term'` —
и упал **целиком**: ошибка привязки имени валит батч, поэтому из пяти блоков не
отработал ни один. Имя взято мной из `dict_registry/fields.csv`, то есть из реестра,
а не из живого аудита — прямое нарушение §4. Кандидат снят, в RPT_D заведён блок 0
(полный список колонок `loans` из `INFORMATION_SCHEMA`), блоки разделены `GO`,
чтобы одно неверное имя стоило одного блока, а не всего прогона. Подробно —
`METRICS.md`, §«Отозванный кандидат».

**Обновлено 23.09.2026 (2):** заведён `METRICS.md` — нормативный справочник метрик.
Наименования приняты в редакции автора от 23.09.2026; ниже по контуру метрика
называется так и только так. Из 21 метрики **10 считаются на сегодняшних данных,
4 ждут выбора поля автором** (прогон `RPT_D`), **7 не считаются** — по каждой названо,
что именно запрашивать и у кого. Слой «риск» закрыт на одну метрику из пяти:
лестницы просрочки в витрине нет, просрочка присутствует как сумма, а не как история.

Попутно исправлено противоречие в таблице ограничений ниже: строка `days_past_due`
по-прежнему отсылала к `delinquency_bucket`, отозванному в том же файле выше.

**Обновлено 23.09.2026:** прогон RPT_A закрыл слой «запас» (`total_balance_debt`)
и **опроверг** два вывода — см. «Два отозванных вывода». Найдена ошибка в собственном
измерении: в RPT_A блок 3 сумма бралась по всем 345 853 счетам без фильтра по статусу,
внутри были закрытые, расторгнутые, списанные и отказные. Полученные 1 523,5 млрд ₸
портфелем не являются, и «расхождение с AQR 39 %» частично создано этим.
Исправление — `RPT_C`.

Попутно: у счёта есть **свой статус `la_status`, уже расшифрованный**. Он закрывает три
открытых вопроса карты статусов — подтверждает `О` = Открыт по S17 (76 290 договоров,
было предположением), и даёт расшифровку двух статусов S02.

## Архитектура: четыре слоя

Схема взята из отраслевого образца (корейский отчёт по автопортфелю) и проверена
на применимость к нашим данным.

| Слой | Что отвечает | Состояние |
|---|---|---|
| **Поток** | что вошло в портфель и что вышло | **строится** — `l_funding_date` заполнена на 100 % по всем четырём источникам |
| **Запас** | что лежит сейчас | строится — `loan_account` с фильтром по статусу |
| **Риск** | что портится | строится на **остатке счёта 1424**, не на полях дней — см. отзыв ниже |
| **Разрезы** | где именно портится | частично, см. ограничения |

Ключевое открытие: **поток строится не сравнением снимков, а по датам внутри одного.**
`loans` историзована на одну дату (01.09.2026), но несёт `l_funding_date`,
`l_first_repayment_date` и `l_actual_closure_date` — вход и выход читаются без истории.

---

## Два отозванных вывода

**Отзыв 1 — провизии в витрине есть.** Прежняя формулировка: «провизии по займам —
нет; есть только по гарантиям и условным обязательствам». Неверно. Они лежат не под
именем, а **счетами**, и формула зарегистрирована в `risk_analytics_data_model.md`:

```
provisions_total = la_account_1428 + la_account_1845 + la_account_18771
```

Поиск по маскам `%provis%`, `%reserv%`, `%ecl%` их не находил. Нашлись только когда
вывели список колонок целиком. Метрика «покрытие провизиями» **разблокирована**:
на 01.09.2026 по счетам в статусе «Открыт» — 113,3 млрд ₸.

**Отзыв 2 — `delinquency_bucket` не является лестницей просрочки.** Прежняя
формулировка: «просрочку считать по `delinquency_bucket` (97,9 % заполнения)».
Заполненность высокая, но смысл не установлен, и данные его опровергают:

| Источник | корзина 1 | корзина 2 | корзина 3 |
|---|---|---|---|
| S01 | 0 … 27 | 0 … 90 | **0** … 6041 |
| S03 | 3 … **950** | 3 … 91 | **1** … 5465 |
| S17 | 6 … 29 | 6 … 90 | **6** … 1778 |
| S02 | `days_past_due` 100 % NULL — проверить нечем |

Диапазоны перекрываются на каждом источнике. Структура корзин тоже различается:
у S01 и S03 есть корзина 4, у S02 и S17 — `NULL`.

**Чем мерить вместо этого: остатком счёта 1424.** По S17 соответствие точное —
`la_account_1424 <> 0` даёт 11 518 счетов, и ровно на этих же 11 518
заполнен `days_past_due_principal`. Признак работает одинаково на всех четырёх
источниках: **37 019** счетов из 345 853 — 10,7 %.

---

## Постоянный набор метрик

Прокатывается по каждому разрезу без изменений — на этом держится сравнимость:

> **EAD · переход 1 → 3 · доля 90+ · покрытие провизиями**

Четвёртая метрика сегодня **не считается** — см. ограничения.

Полный перечень с определениями и вердиктом по каждой — в `METRICS.md`.

---

## Отбор действующего договора

Источник правила — `dict_registry/status_map.csv`.

```sql
      (   (l.l_source = 'S01' AND l.l_loan_status = N'О')      /* CP1251 206 */
       OR (l.l_source = 'S02' AND l.l_loan_status = 'Account OK')
       OR (l.l_source = 'S03' AND l.l_loan_status = 'O')       /* латиница 79 */
       OR (l.l_source = 'S17' AND l.l_loan_status = N'О') )    /* CP1251 206 */
  AND l.l_actual_closure_date IS NULL
```

Два условия вместе, а не порознь. Даёт **541 416** договоров из 8 231 917 (6,6 %).

**Правило заведомо занижает портфель** на договоры неопределённого статуса: **4 756**
(редакция 23.09.2026 — «примерно 32 900»; из `status_map.csv` не воспроизводилось,
было 28 542, из них 23 786 — S17 `Т`, переведённый в «закрыт» прогоном DICT_D). Недобор виден при сверке с расчётом провизий, перебор — нет.

**Гомоглифы.** Латинская `O` (код 79) у S03 и кириллическая `О` (206) у S01 и S17
на экране неразличимы. Условие по источнику обязательно; общее `IN ('O', N'О')` —
источник ошибки, которая ничем себя не выдаёт.

---

## Ограничения, установленные аудитом

| Что | Состояние | Следствие |
|---|---|---|
| **Стадии 1/2/3** | в витрине **нет** ни одного поля | доля Stage 3 не считается |
| **`delinquency_bucket`** | заполнен на 97,9 %, но **смысл не установлен**: корзина 1 по S03 содержит просрочку до 950 дней, корзина 3 — от 1 дня. Диапазоны перекрываются на каждом источнике, где `days_past_due` вообще есть | лестницей просрочки не является |
| **`days_past_due_principal`** | по S02 заполнен на 100 % и **везде равен нулю** | заполненность без содержания опаснее NULL |
| **Размер предприятия** | `l_entrepreneur_category` — это ФЛ/ЮЛ, **не крупный/средний/малый** | сегменты **CORLAR, CORMED, RETSML** не размечаются |
| `entity` | нет | правило DISASS — но оно инертно (ТР18) |
| `f_inv` | нет | CORINV — но признак не проставлен ни разу (О3) |
| `b_lsboo` | **код связи**, не флаг: заполнен у 2 765 из 15,64 млн | RELATE = `IS NOT NULL`, не `= 1`. Коды не расшифрованы |
| `days_past_due` | заполнен на 16,9 % | лестница просрочки не строится ни по нему, ни по `delinquency_bucket` (отзыв 2). Просрочка меряется суммой — остатком счёта 1424 |
| `b_region` | 1 087 значений, 35 % пусто | разрез по городам не строится |
| `l_branch_code` | один код даёт до 17 названий | разрез по филиалам не строится |
| `l_financial_consultant` | только S03 и только 47,7 % | разрез по менеджерам — на половине одного источника |
| `l_first_repayment_date` | по S02 — **ноль** | FPD по картам не считается |
| `l_initial_fee` | практически только S17 | разрез по первоначальному взносу — один источник |
| `l_rate` | содержит название продукта, а не ставку | доходность не считается |

**Вывод по сегментации:** из тринадцати правил размечаются десять. Три корпоративных
упираются в отсутствующее поле размера предприятия, и вычислить его по ст. 24 ПК РК
нельзя — численность и доход в витрине отсутствуют (О2).

---

## Состав

| Файл | Что |
|---|---|
| `REPORT_STRUCTURE.md` | **форма отчёта** — строки, столбцы, порядок разделов, сборка, привязка слоёв к месяцу |
| `METRICS.md` | **справочник метрик** — нормативные наименования, определения, формулы и вердикт по каждой из 21 метрики |
| `sql/RPT_A_loan_account_audit.sql` | аудит `loan_account` — последняя неаудированная таблица слоя «запас» |
| `sql/RPT_C_portfolio_reconciliation.sql` | приведение цифры витрины к сопоставимому виду и разложение расхождения с AQR |
| `sql/RPT_D_flow_field_audit.sql` | аудит полей-кандидатов под разделы 2-1, 2-3 и 3 — разрешает четыре развилки `METRICS.md` |
| `powerbi/BUILD.md` | **сборка отчёта в Power BI шаг за шагом**, контрольные клетки |
| `powerbi/PBI_model.dax` | весь DAX модели одним файлом — вставлять отсюда |
| `powerbi/POWERBI.md` | **модель Power BI**: витки, таблицы фактов, меры DAX (запас — полуаддитивно, заёмщики — не складываются между источниками), контроли с известным ответом |
| `powerbi/PBI_0_preflight.sql` | один раз до подключения: сетка срезов «месяц × источник», домен `la_status`, продуктовые поля для витка 2 |
| `powerbi/PBI_fact_flow.sql`, `powerbi/PBI_fact_stock.sql` | факты потока и запаса — вставляются в Power BI как есть, режим импорта |
| `powerbi/PBI_fact_borrowers.sql` | заёмщики уровня источника — с витка 2 они не складываются между продуктами |
| `sql/RPT_H_card_limits.sql` | Р-8: падение среднего лимита на новую карту S02 — доля карт с лимитом против уровня лимита, статус нулевых, был ли у них остаток; поиск истории лимита в витрине |
| `sql/RPT_G_stock_jumps.sql` | три скачка, которые в Power BI выглядят событиями: S17 12.2025 (Р-4), провизии S03 06.2026 (Р-5), счета без договора |
| `sql/RPT_F_writeoff_months.sql` | были ли у CrediLogic списания в шести «пустых» месяцах — проверка устного подтверждения на данных |
| `VINTAGE_PLAN.md` | **план кривых поколений выдач**: накопленная доля просрочки по месяцам жизни когорт; развилки В-1…В-4 |
| `SEGMENT_TREE_PLAN.md` | **план разметки строк деревом решений**: цель — сегмент, утверждённый АФР; критерии до прогона; развилки Д-1…Д-5 |
| `sql/TREE_0_link_audit.sql` | разведка перед деревом: ключ и покрытие связи меток АФР с витриной, представительность, происхождение `l_segment` |
| `sql/RPT_E_segment_mapping_audit.sql` | чем размечается **строка** отчёта — семь кандидатов, решающий блок 1 по `l_segment` |

---

## Связи

| Контур | Что берём |
|---|---|
| `dict_registry` | `status_map.csv` — отбор действующих; вердикты по полям |
| `risk_dwh_reconciliation` | `FINDINGS.md` — ground truth по витрине |
| `nst_credit` | тринадцать правил сегментации и их основания |

---

## Гипотезы

§5: гипотеза нумеруется и живёт в реестре контура.

| № | Гипотеза | Основание | Чем проверяется | Состояние |
|---|---|---|---|---|
| Р-3 | Обрыв выдач S03 с апреля 2026 — прекращение выдач, а не дефект загрузки | ряд выдач: 5 392 → 523 → 13 | ответ автора | **закрыта ответом автора** 24.09.2026: выдачи прекращены. На данных — только сам обрыв и последующее сокращение остатка на 27–33 млрд в месяц |
| **Р-4** | S17, декабрь 2025: ~30 тыс. «погашений» — выбытие проблемного портфеля, записанное статусом «Закрыт», а не погашение | закрытий 35 679 при обычных 3–4 тыс.; просроченных договоров −31 549; провизии −11 млрд; списаний у S17 нет в принципе (CL-S17-02) | `RPT_G` блок 1: доля просроченных на начало месяца у закрытых в декабре против октября и ноября; собранность дат закрытия | **подтверждена** 24.09.2026: 86,2 % против 7,9–10,2 %; 55 % одним днём. Канал выбытия (продажа, уступка) данные не называют |
| **Р-5** | S03, июнь 2026: провизии −33,9 млрд — пересчёт модели либо перенос на счёт вне формулы 1428 + 1845 + 18771 | ни пачки списаний, ни ухода просрочки; остаток сократился как обычно | `RPT_G` блок 2: все колонки `la_account_*` на 01.06 и 01.07.2026 | **закрыта: перенос** 24.09.2026. Счета 1484 (+29,6) и 1485 (+29,1 млрд) появились с нуля. Вторая ветка — пересчёт модели — опровергнута |
| Р-6 | Провизии S03 переехали с `1428` на `1484` | `1428` за вычетом обычного сокращения −29,7 млрд, `1484` +29,59 | ответ бухгалтерии | **ослаблена** 24.09.2026: по продуктам не держится — у новых авто `1484` на 7,5 млрд больше падения провизий, у прочих меньше. `1485` на 97,8 % — новые автокредиты. Одновременно упали `1434` (дисконты) и `1860` (комиссии). Открыта до ответа бухгалтерии |
| **Р-7** | Новый продукт Fenix (`S17|—|—`, продуктовые поля пусты) — автокредиты, перенесённые из CrediLogic | чек 5,6–8,3 млн (денежные S03 — 0,8–2,4), срок 63–80 мес (денежные — 31–43, авто — 75–84); выдачи S03 NewCar и S17 вместе ровны до 03.2026 | ответ владельца Fenix | открыта |
| **Р-8** | Падение среднего лимита на новую карту S02 — артефакт снимка (`loans` — один срез на 01.09.2026, `l_loan_amount` у S02 — текущий лимит; свежим картам лимит ещё не установлен), а не смена продукта или политики | лимиты ÷ выдачи по месяцам: 103 тыс. ₸ (09.2025), 60–97 тыс. (10.2025–02.2026), 36 → 19 → 24 → 14 → 12 тыс. (03–07.2026), 1,2 тыс. (08.2026). По всем S02 `l_loan_amount` нулевой в 79,6 % (RPT_D) | по месяцу выдачи: доля карт с лимитом > 0 и средний лимит среди них. Плавный рост доли нулевых к дате снимка — поддерживает (лаг установки лимита); ступенька в одном месяце при ровной доле до и после — опровергает (смена продукта) | **(а) не поддержана, (б) поддержана, (в) доказана как механизм, но падения не объясняет; по (г) правило не сработало** — `RPT_H`, 24.09.2026. Самопроверка сошлась: 760 карт, 78,57 млн ₸ в 09.2025. (а) Доля карт с лимитом > 0: 13,0 % в 10.2025–03.2026 (345 из 2 657), 1,8 % в 04–08.2026 (27 из 1 465). После обрыва не догоняет с возрастом: когорта 04.2026 в 5 мес — 2,2 %, 08.2026 в 1 мес — 1,3 %. У 1 438 нулевых карт 04–08.2026 остатка не было ни у одной. Строго одним снимком (а) не отвергается: возраст и когорта в нём неразделимы, а истории лимита в витрине нет (блок 0б). (б) Ступенька 03→04.2026 (9,6 % → 2,2 %), дальше ровно 1,3–2,6 %; по времени совпадает с сокращением розницы в апреле 2026 (Р-3, сноска «выдачи, 04.2026») — совпадение, не причина. (в) Закрытые карты — лимит 0 у 100 % во всех 24 когортах; заблокированные («прочие») лимит сохраняют — нулевых среди них нет. В 04–08.2026 закрытых 0–4 на когорту, падение не объясняет; зато суммы лимитов старых когорт занижены закрытиями. (г) Правило, записанное до прогона, — «ведущий множитель — чьё отношение 08.2026/09.2025 дальше от 1» — указывает на уровень лимита (0,067 против 0,179 у доли). Правило дефектно: концы на 56 и 3 картах, а 09.2025 — пик крупных лимитов (13 карт > 1 млн). Замена правила после прогона — post hoc, поэтому это не «(г) опровергнута», а «правило ответа не дало». Для справки, без силы доказательства: на объединённых периодах средний лимит среди > 0 — 562 тыс. → 808 тыс. ₸ на 27 картах, падение целиком в доле. **Попутно, доказано:** и до 04.2026 71–93 % выданных карт S02 — без лимита (84–90 % в 10.2025–03.2026); остаток за жизнь был у 1,6 % из них против 73 % у карт с лимитом (10.2025–03.2026). «Количество выдач» по картам считает в основном не кредитные карты — развилка К-1 за автором |

## Открытое

| № | Вопрос | Кому |
|---|---|---|
| 1 | Где лежит размер предприятия — крупный, средний, малый | владелец витрины |
| 2 | Расшифровка кодов `b_lsboo` | владелец витрины |
| 3 | Справочник статусов S02 (11 значений) и S17 (3 значения, 488 115 договоров) | владельцы систем |
| 4 | Статус `Д` по S01 — 149 договоров; заявлено «других статусов нет» | владелец РС |
| 5 | Откуда брать стадии и провизии, если в витрине их нет | расчёт провизий |
| 6 | Что такое счета **1484** и **1485**, с какой даты ведутся и входят ли в провизии. С июня 2026 на них ~58,7 млрд по S03; `1485` на 97,8 % — новые автокредиты. Не переехали ли туда дисконты (`1434`) и комиссии (`1860`) | бухгалтерия / владелец витрины |
| 8 | Какой продукт выдаётся в Fenix с апреля 2025 без продуктовых полей (Р-7) | владелец Fenix |
| 7 | Каким каналом выбыли ~30 тыс. договоров S17 в декабре 2025 (Р-4) — продажа, уступка, иное | владелец Fenix / казначейство |
| 9 | **К-1.** Что считать выдачей карты в 2-1 ②: все договоры S02 (как сейчас; 71–99 % без лимита), только с лимитом > 0, или обе строки — «выдано карт» и «в т.ч. с лимитом» | автор |
