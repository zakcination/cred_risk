# Источники данных контура dpd15_cure: что откуда берётся

Составлено 27.08.2026 после отзыва результатов D15-G. Читать **до** любого
расчёта: половина ошибок контура возникла не в логике, а в понимании источника.

Всё ниже — из живого аудита `INFORMATION_SCHEMA` и прогонов, не из памяти
и не из BI-канваса.

---

## 1. Схема

```
                    ┌───────────────────────────────────────────────┐
                    │  [CL_PORTFOLIO].[dbo].[HISTORY_DEFAULT_ACCOUNT]│
                    │  ЗЕРНИСТОСТЬ: 1 строка = 1 счёт  (проверено)   │
                    │  6 756 640 счетов, 188 колонок                 │
                    ├───────────────────────────────────────────────┤
                    │ account_number   nvarchar(50)   ← КЛЮЧ         │
                    │ default_date     date  ПЕРВЫЙ дефолт          │
                    │ health_date      date  ПЕРВЫЙ выход            │
                    │ new_default_date date  ПОСЛЕДНИЙ дефолт;       │
                    │                        ОБНУЛЯЕТСЯ при выходе   │
                    │ new_health_date  date  ПОСЛЕДНИЙ выход         │
                    │ fact_close_date  date    фактическое закрытие  │
                    │ type             nvarchar(50)                  │
                    │ ['01.02.2012'] … ['01.02.2027']                │
                    │   181 колонка nvarchar(50) = ПОМЕСЯЧНЫЙ DPD    │
                    └───────────────┬───────────────────────────────┘
                                    │ account_number = contract_number
                                    │ пересечение 270 713 из 6 756 640
                                    ▼
      ┌─────────────────────────────────────────────────────────┐
      │  [CL_PORTFOLIO].[dbo].[CL_PORTFOLIO_2]                   │
      │  ЗЕРНИСТОСТЬ: 1 строка = счёт × отчётная дата (проверено) │
      ├─────────────────────────────────────────────────────────┤
      │ contract_number   varchar   ← КЛЮЧ                       │
      │ date              date      отчётная дата, 1-е число     │
      │ dpd               numeric   просрочка на дату            │
      │ category          numeric   корзина 1 / 2 / 3            │
      │ tag               varchar   '11' = списанные             │
      │ balance, balance_with_discount, provisions_total          │
      │ ПРОДУКТ — поле НЕ НАЙДЕНО, ищется § 4 D15_H_controls.sql │
      └─────────────────────────────────────────────────────────┘
                                    │
                                    │ account_number
                                    ▼
      ┌─────────────────────────────────────────────────────────┐
      │  [IFRS9].[dbo].[KAN_<YYYYMMDD>_for_LGD_Fenix]            │
      │  источник PD и LGD. РЯД ЕЖЕМЕСЯЧНЫЙ, брать самую свежую  │
      │  (на 27.08.2026 — KAN_20260801). Парная _restructura     │
      │  ЗЕРНИСТОСТЬ: 652 168 счетов / 652 169 строк, max 2      │
      ├─────────────────────────────────────────────────────────┤
      │ account_number            ← КЛЮЧ                         │
      │ IIN                       ← ПЕРСОНАЛЬНЫЕ ДАННЫЕ,         │
      │                             SELECT * ЗАПРЕЩЁН            │
      │ subproduct nvarchar(50)   ← ПРОДУКТ: POS, UnCL, Payroll, │
      │                             Loyalty, NCL, UCL и ещё 9    │
      │ TARIFF                    дробнее продукта               │
      │ FIRST_PAYMENT_DATE        ← ДЕНЬ ПЛАТЕЖА для ВСЕХ, в т.ч.│
      │                             непросроченных               │
      │ status, DURATION, LOAN_AMOUNT, activation_date           │
      │ default_date, default_date_old, health_date2             │
      │ [дата окончания реструктуры], poci_date, bankrupt_date   │
      │ dead_convict                                             │
      │ trigger_первый / trigger_последующий /                   │
      │ [trigger на тек дату]     ← ОПРЕДЕЛЕНИЕ ДЕФОЛТА В МОДЕЛИ │
      └───────────────┬─────────────────────────────────────────┘
                      │ subproduct = Product
                      ▼
      ┌─────────────────────────────────────────────────────────┐
      │  [IFRS9].[dbo].[KAN_PD_LGD_Product_Basket]               │
      │  Product -> Basket -> PD, LGD, [LGD+TV]; Date            │
      │  привязка продукта к корзине AQR, меняется во времени    │
      └─────────────────────────────────────────────────────────┘
      ┌─────────────────────────────────────────────────────────┐
      │  [IFRS9].[dbo].[KAN_contract_basket]                     │
      │  contract -> basket на date; корзина по договору         │
      └─────────────────────────────────────────────────────────┘

      ┌─────────────────────────────────────────────────────────┐
      │  [Dictionaries].[risk_analytics].[repayment_schedule]    │
      │  график платежей; НЕ ИСПОЛЬЗОВАН, ключ не подтверждён    │
      ├─────────────────────────────────────────────────────────┤
      │ rs_loan_id, rs_dog_num — что из них ключ, НЕ ИЗВЕСТНО    │
      │ rs_repayment_date, rs_report_date (несколько версий?)    │
      └─────────────────────────────────────────────────────────┘
```

---

## 2. Что и чем подтверждено

| Утверждение | Как проверено | Результат |
|---|---|---|
| `HISTORY_DEFAULT_ACCOUNT` — одна строка на счёт | `GROUP BY account_number`, max | `max_rows_per_account = 1`, 6 756 640 счетов |
| Помесячная сетка содержит **DPD** | сверка с `CL_PORTFOLIO_2.dpd` на двух датах | 229 207 из 229 207 и 254 491 из 254 491, ноль нечитаемых |
| Сетка — **не** корзина | та же сверка с `category` | 7 совпадений из 229 207, то есть случайные |
| Ключи стыкуются | `LEFT JOIN` по `account_number = contract_number` | 270 713 счетов в портфеле за окно |
| `CL_PORTFOLIO_2` уникальна по счёт × дата | `GROUP BY` + max | `max_rows_per_account_month = 1` |
| Глубина истории дефолтов | `MIN/MAX(default_date)` | 2011-12-01 … 2026-07-01 |
| Дефолтнувших счетов | `default_date IS NOT NULL` | **547 580** из 6 756 640 |

---

## 3. Механизм ведения базы дефолтов

Со слов владельца процесса, 27.08.2026:

> Ежемесячно берутся займы, дефолтнувшие **семь месяцев назад**, проверяется
> их DPD; при **шести чистых месяцах подряд** дата дефолта обнуляется
> и проставляется `health_date`.

**Следствия, обязательные к учёту:**

1. **Чистый прогон заканчивается самой `health_date`,** а не месяцем перед ней.
   Окно наблюдения — `[health_date − 5; health_date]` включительно.
   Именно на этом сорвался D15-G: окно `[health_date − 6; health_date − 1]`
   захватывало последний дефолтный месяц. Точное смещение измеряется
   профилем § 1 в `D15_H_controls.sql`, а не принимается на веру.

2. **«Дата дефолта обнуляется».** Значит счёт с `health_date` может иметь
   `default_date = NULL`. Фильтр D15-G требовал непустыми **обе** даты
   и потому отбирал не всех оздоровлённых. Знаменатель всех ставок под
   вопросом до прогона § 0 `D15_H_controls.sql`.

3. **Минимальный срок до оздоровления — семь месяцев** от даты дефолта.
   Счёт с `health_date − default_date < 7` месяцев означает иной путь
   (списание, продажа, ручная правка), а не оздоровление по правилу.

### 3.1. Что означает сама дата дефолта. Замер 31.08.2026

Срез `CL_PORTFOLIO_2.dpd` на `new_default_date`, все 12 522 срыва:

| DPD на дату дефолта | счетов | доля |
|---|---:|---:|
| 61–90 | 10 325 | **82,5 %** |
| 0 | 1 858 | 14,8 % |
| 31–60 | 270 | 2,2 % |
| 1–30 | 8 | 0,1 % |
| нет среза | 61 | 0,5 % |

**Дата дефолта ставится на отчётный месяц, в течение которого заём пересекает
90 дней.** Снимок на начало этого месяца показывает 61–90, 91-й день
наступает внутри месяца.

Два следствия, обязательных к учёту:

1. **На м1 после даты дефолта заём уже 91+.** Окно наблюдения, отсчитанное
   как м1…м6, включает заведомо грязный месяц. Первое достижимое чистое
   окно — **м2…м7**, что совпадает с минимальным месяцем выхода 7,
   измеренным в `D15_N_run.sql` § 1. На этом сорвался `D15_M_run.sql`.

2. **Реестр регистрирует не только 91+.** 1 858 счетов (14,8 %) получили
   дату дефолта при DPD = 0 — неплатёжные признаки Приложения 4 п. 5:
   реструктуризация с ухудшением, смерть, лишение свободы, банкротство.
   Прежняя формулировка «только по просрочке» **неверна** и исправлена
   в `RESEARCH_PROTOCOL.md` § 2: периметр узкий на 85 %, а не на 100 %.

---

## 4. Три конфаундера, любой из которых портит расчёт

| Конфаундер | Почему опасен | Источник | Статус |
|---|---|---|---|
| **Приостановка начисления** | DPD = 0 всё время приостановки; счёт проходит любой критерий чистоты, **не заплатив ничего** | поле не найдено | **открыт.** Косвенный тест — § 3d: чистое окно при неизменном балансе |
| **Закрытие договора** | закрытие внутри окна — не оздоровление | `fact_close_date` | закрывается § 3a |
| **Реструктуризация** | меняет график, обнуляет накопленную просрочку | `[дата окончания реструктуры]` в IFRS9 | закрывается § 3b, зернистость таблицы не проверена |

Приостановка — самый опасный из трёх и единственный незакрытый.
`sql/stage3_raw_extract.sql` § 4 фиксирует, что поле под неё в репозитории
не встречалось ни разу. Пока поле не найдено, **приостановленные счета
из расчёта не исключены, и это надо говорить прямо, а не умалчивать.**

---

## 5. Чего в источниках нет

| Требуется | Для чего | Есть? |
|---|---|---|
| ~~Продукт / портфель~~ | разрезы по продуктам | **НАЙДЕН 27.08.2026**: `subproduct` в IFRS9; корзина AQR — через `KAN_PD_LGD_Product_Basket` |
| ~~День планового платежа~~ | П3, целевая дата переноса | **НАЙДЕН**: `FIRST_PAYMENT_DATE`, для всех договоров |
| Сегмент НСТ | второй разрез | правила в `nst_credit/nst_fill_2026.py`, соединение не построено |
| Валовая балансовая стоимость на момент входа в дефолт | условие п. 15 абз. 1 № 61 по сумме | нет |
| Дата и сумма фактического платежа | П1: просрочка взноса вместо среза | `repayment_schedule`, ключ не подтверждён. Для дня платежа больше не нужен — есть `FIRST_PAYMENT_DATE` |
| Тип заёмщика ФЛ / ЮЛ, база оценки | поблажка п. 15 пп. 1 только для ФЛ на коллективной основе | нет |
| Число эпизодов дефолта | повторность как счётчик | **структурно невозможно**: база хранит ПЕРВЫЙ и ПОСЛЕДНИЙ, промежуточные не видны. Выводится только «один эпизод / более одного» |

Последняя строка означает, что любая величина повторного дефолта из этого
источника — **нижняя граница**, и предъявлять её надо так.

---

## 6. Порядок работы, который следует из всего перечисленного

1. `D15_H_controls.sql` § 0 — определение популяции. **Блокирует всё остальное.**
2. § 1 — измерение смещения окна по профилю нулей.
3. § 2 — классификация на измеренном окне; контроль: группа `strict`
   обязана стать непустой.
4. § 3 — конфаундеры; приостановленные и закрытые исключаются из знаменателя.
5. § 4 — поиск поля продукта.
6. § 5 — разрезы продукт × порог, с правилом минимальной клетки в 100 займов,
   утверждённым **до** расчёта.

Ни одна цифра не выносится за пределы контура раньше шага 4.

---

## 7. Определение дефолта в модели PD/LGD против Методики

Из `trigger_первый` / `trigger_последующий` / `[trigger на тек дату]`,
прогон 27.08.2026:

| Триггер модели | Признак Приложения 4 п. 5 |
|---|---|
| `91+` | пп. 1, просрочка более 90 дней |
| `реструктуризация с фин. ухудшением` | пп. 2 |
| `смерть заемщика` | пп. 3 |
| `тюремщик` | пп. 4, места лишения свободы |
| `банкротство` | пп. 5 |

**Все пять признаков Методики присутствуют, шестого нет.** POCI — отдельная
категория МСФО 9, а не дополнительный триггер. По существу определение дефолта
в модели и в учёте совпадает; Н5/Н8 в части перечня событий **не подтверждается**.

Три дефекта данных, видных при этом:

1. **58 130 счетов** с пустым первым и последующим триггером при заполненном
   текущем — модель не хранит причину первого дефолта.
2. **`poci_date` заполнена у нуля счетов** при 2 833 счетах с триггером POCI.
3. **Регистр не нормализован**: «тюремщик» и «Тюремщик», «с фин. ухудшением»
   и «с фин. Ухудшением». Группировка без `UPPER`/`TRIM` дробит категории.

Расхождение по датам дефолта проверено дважды и **находкой пока не является**:
после `COALESCE(default_date, default_date_old)` совпадений 19 444 из 29 265
(66,4 %) против 5 857 до. Остаток проверяется § 1f `D15_J_products.sql` —
гипотеза, что база хранит ПЕРВЫЙ дефолт, а IFRS9 ПОСЛЕДНИЙ, и сравнивались
разные события.
