# CLAUDE.md — cred_risk (БРМ, АО «Евразийский банк»)

Репозиторий рабочих контуров БРМ: сверка DWH, лимиты, риск-аппетит, классификация документов.
Один автор (Мирас), внешний ревьюер по одному контуру (Сайлау), **CI нет** — ни один PR
не проверяется автоматически. Всё, что ниже, написано под это, а не под общие практики.

Контурные CLAUDE.md имеют приоритет над корневым в своей папке.

### Кто есть кто

Имена в репозитории путались — исправлено 24.08.2026. Различать обязательно:
роль определяет, чей вход принимается как данность, а чей обсуждается.

| Имя | Роль | Где встречается |
|---|---|---|
| **Мирас** | автор репозитория | везде |
| **Дамир** | руководитель | — |
| **Sabila** | заместитель руководителя (после Дамира) | контур `nst_credit`: список индивидуальных заёмщиков (66 БИН), расчёт СК, разбор AQR2026. Её вход **принимается неизменным** |
| **Сайлау** | коллега, не руководитель | контур `risk_dwh_reconciliation`: внешний ревьюер, прогон №2 по PR #19 |

Sabila и Сайлау — **разные люди в разных контурах**. Список индивидуальных
заёмщиков — от Sabila; ревью сверки DWH — от Сайлау.

---

## 1. Ветвление

### Почему правила именно такие

Имя ветки задаёт харнесс при старте сессии (`claude/<slug>-<хеш>`), а не автор. Слаг привязан
к первой задаче сессии и дальше не меняется, поэтому **имя ветки не описывает её содержимое**
и описывать не будет. Факты по репозиторию на 18.08.2026: ветка
`claude/database-table-analysis-wxxgtv` за месяц провела 9 PR по трём несвязанным темам —
инвентаризация DWH, отзыв находки по grain, лимиты НЗ/СП. Ветка
`claude/cred-risk-topic-classifier-1cjbtk` — 8 PR, от классификатора документов до Phase C/D.

Отсюда правила не про имена, а про поведение: **носителем смысла является PR и путь
`docs/analysis/<контур>/`, а не имя ветки.**

### Правила

**Б1. Один PR — один контур.** Все файлы PR лежат под одним `docs/analysis/<контур>/`
(либо под одним каталогом верхнего уровня: `sql/`, `topic_classifier/`, `notebooks/`).
Проверка перед пушем:

```
git diff --name-only origin/main...HEAD | cut -d/ -f1-3 | sort -u
```

Больше одной строки — PR надо разделить. Смешанный PR нельзя ни отревьюить, ни откатить.

**Б2. Ветка после слияния мертва.** Как только PR смёрджен, ветка не продолжается.
Следующая задача начинается с

```
git fetch origin main && git checkout -B <та-же-ветка> origin/main
```

Продолжение работы поверх смёрдженной истории — источник PR, диффы которых нельзя прочитать.
Если на ветке остались **не** слитые коммиты — их сначала перебазировать на новый main,
а не сбрасывать.

**Б3. Ветка живёт до 7 дней и до ~20 коммитов.** Больше — либо приземлять, либо разделять.
Наглядный контрпример в самом репозитории: `risk-dwh-bi-canvas` — 70 коммитов, +16 141 строка,
34 файла, открыта с 30.07, отстала от main на 20 коммитов. Такой PR физически неревьюабелен,
и каждый день его существования увеличивает стоимость слияния.

**Б4. Заблокированный PR синхронизируется с main еженедельно.** Если PR ждёт человека,
это не повод давать ему протухать: `Update branch` на GitHub либо merge main в ветку.
Конфликты дешевле разбирать по одному, чем все сразу через месяц.

**Б5. Кто мёрджит.** GitHub **не разрешает автору апрувить собственный PR** — попытка
возвращает `Can not approve your own pull request`. Поэтому для одиночной работы схема такая:
draft → самопроверка → снять draft → merge. Ждать апрува, которого не может быть, нельзя.

**Исключение, которое важнее правила:** если в PR назначен живой ревьюер (`requested_reviewers`)
или в теле PR есть незакрытые пункты, отмеченные его именем, — **PR не мёрджится без него**,
сколько бы он ни висел. Пример: PR #19 ждёт прогон №2 от Сайлау; слить его в одиночку —
значит внести в main 71 SQL-проверку, из которых шесть уже один раз оказались багами скрипта,
а не дефектами марта.

**Б6. Метод слияния — merge commit** (не squash, не rebase). История PR в этом репозитории
сама по себе документ: по ней видно, где находка была отозвана (PR #14 → #15).

**Б7. Ветки, слитые полностью, удаляются — вручную либо автоматически.**
Токен агентской сессии **не имеет права удалять ссылки** на origin: `git push --delete`
возвращает `HTTP 403`, а GitHub MCP не отдаёт инструмента удаления. Поэтому уборка —
на стороне человека, и правильный способ один: включить в
**Settings → General → Pull Requests → Automatically delete head branches**. Тогда ветка
исчезает в момент слияния PR и список не нужно чистить руками.

Отдельно: ветки, выданные харнессом (`claude/<slug>-<хеш>`), **появятся снова** — их создаёт
следующая сессия. Это нормально и не отменяет правила: такая ветка не продолжается, а
пересоздаётся от main по Б2.

Проверка списка мёртвых веток:

```
git fetch --prune origin
git for-each-ref --format='%(refname:short)' refs/remotes/origin | while read b; do
  if [ "$b" != origin/main ] && [ "$(git rev-list --count origin/main..$b)" = 0 ]; then
    echo "мертва: $b"
  fi
done
```

**Б8. Что актуализируется в момент слияния.** PR не закрыт, пока не обновлены документы,
которые он делает неверными. Список выведен из того, что в репозитории уже ведётся, —
не из общих практик.

| Что обновить | Когда | Почему именно это |
|---|---|---|
| **Реестр находок контура** (`FINDINGS*.md` либо `README.md` контура): строка `Обновлено <дата>:` с перечнем изменённого | PR меняет статус находки, гипотезы или вопроса | Приём уже ведётся в `FINDINGS_RA.md`. Без него не отличить, какая часть реестра описывает сегодняшний день, а какая — состояние трёхмесячной давности |
| **Дата в заголовке реестра**, если он датирован | Появились новые подтверждения на данных | `FINDINGS.md` озаглавлен «Ground Truth (доказано на данных, 01.07.2026)». Дата в заголовке, которую не двигают, вреднее её отсутствия: она заявляет свежесть, которой нет |
| **§3 «Карта репозитория»** — строка нового контура | PR заводит новый `docs/analysis/<контур>/` | На 01.09.2026 §3 знает три контура, а работа идёт в семи: `nst_credit`, `dpd15_cure`, `risk_showcase`, `bank_recovery` в карте отсутствуют |
| **`sql/README.md`** | PR добавляет, переименовывает или удаляет скрипт в `sql/` | Самый правимый файл репозитория — 22 правки. Индекс скриптов полезен ровно до тех пор, пока его ведут |
| **README соседнего контура** | Находка закрывает вопрос в другом контуре | Это уже требование §3; в момент слияния оно становится проверяемым шагом, а не пожеланием |
| **Корневой `README.md`** | PR меняет то, **чем является** репозиторий | Сейчас он озаглавлен «Credit-Risk Document Topic Classifier» и описывает только классификатор. Это перестало быть правдой на втором контуре |

Отдельно про отзыв. Если PR отзывает ранее сделанный вывод, недостаточно поправить текст:
§5 требует отдельного коммита, а реестр обязан сохранить **и прежнюю формулировку, и причину
отзыва**. Стереть неверный вывод — значит потерять доказательство того, что он проверялся;
именно на этом стоит Б6.

Проверка перед снятием draft — какие индексы затронуты:

```
git diff --name-only origin/main...HEAD | grep -Ei 'FINDINGS|README|CLAUDE\.md' \
  || echo 'ни один индекс не тронут — свериться с Б8'
```

Пустой вывод не запрещает слияние: PR, не меняющий ни одного вывода, ничего и не
устаревает. Он означает «проверь сознательно», а не «допиши что-нибудь».

### Что НЕ является правилом

Здесь нет develop/release/hotfix, нет запрета на прямой коммит в ветку, нет требования
линейной истории и нет обязательного ревью. Один автор без CI — вводить это значит
имитировать процесс, а не иметь его.

---

## 2. Конфиденциальность — жёстко

**Не попадает в репозиторий ни при каких условиях:**

- Исходные ВНД Банка (Стратегия, Политика, Методика, приложения, протоколы) — промаркированы
  «ВНУТРЕННЯЯ ИНФОРМАЦИЯ» / «конфиденциально». В репозитории только **результаты анализа
  со ссылками на пункты**.
- PII: ИИН, ФИО, номера договоров, IBAN, телефоны. Только агрегаты.
- Выгрузки из промышленных систем и оперативные файлы подразделений. Код сбора — да,
  собранные данные — нет. Ноутбуки коммитятся **без сохранённых выводов**.
- Учётные данные, строки подключения, содержимое `.env`.

Публичные НПА (постановления АРРФР/НБ РК) цитировать можно, но с указанием редакции
и рег. номера.

Перед коммитом бинарников и .docx — открыть и посмотреть, что внутри. `.docx` не диффится,
поэтому попавший в него лишний лист незаметен в PR.

---

## 3. Карта репозитория

| Путь | Контур | Свой CLAUDE.md |
|---|---|---|
| `docs/analysis/risk_dwh_reconciliation/` | Сверка `Dictionaries.risk_analytics` ↔ `CL_PORTFOLIO.dbo`. `FINDINGS.md` — ground truth, читать первым | **да** |
| `docs/analysis/risk_appetite/` | Пересмотр уровней риск-аппетита по кредитному риску (замечание СВА, сроки 30.09 / 31.12.2026) | нет |
| `docs/analysis/npl_limits/` | Лимиты главы 11 Нормативов № 85, сработка лимита 98-3 (НЗ/СП ≥ 10 %) | нет |
| `docs/analysis/dpd15_cure/` | Оздоровление розничных займов при пороге DPD 15 дней: норма, метод сбора на месячных срезах, ретро-тест повторного дефолта. `FINDINGS_DPD15.md` — реестр, `RESULTS_D15N.md` и `RESULTS_D15O.md` — действующие результаты | нет |
| `topic_classifier/`, `topics/` | Классификатор документов кредитного риска | нет |
| `sql/` | Проверочные и сверочные скрипты | правила ниже |
| `notebooks/`, `scripts/`, `data/`, `examples/`, `tests/` | Обвязка контуров | нет |

Контуры связаны: статус системной значимости Банка открыт одновременно в `risk_appetite` (Г12)
и в `npl_limits` (п. 108); определение НЗ по № 61 совпадает с периметром Stage 3.
Находку, закрывающую вопрос в одном контуре, отражать в README соседнего.

---

## 4. Правила SQL (действуют во всём репозитории)

- **Read-only.** Только SELECT. Запрещены INSERT/UPDATE/DELETE/MERGE/DROP/ALTER постоянных
  объектов. Только `#temp`, имена — с префиксом файла.
- **MAXDOP 1** на тяжёлых запросах.
- **PII не выводить.** Только агрегаты; худшие случаи — бакетами величины, не идентификаторами.
- **Только трёхчастные имена:** `[Dictionaries].[risk_analytics].[X]`.
- **Имена колонок — только из живого аудита** (`INFORMATION_SCHEMA`), не из BI-канваса
  и не из памяти. Имя колонки ≠ семантика.
- `CROSS APPLY (VALUES …)` ставить **после** фильтра.
- Сначала агрегат, потом детализация. Не `SELECT *`, не `FORMAT` на больших выборках.

Развёрнутые правила движка, порядок проверки и антипаттерны —
в `docs/analysis/risk_dwh_reconciliation/CLAUDE.md`.

---

## 5. Стиль ответов

Прежде чем поддержать позицию — сильнейший контраргумент против неё. Критерий — точность,
не одобрение. Русский, коротко, без воды. RU-анализ + EN IFRS-термины
(PD / LGD / EAD / ECL / SICR / stage).

Выводы разделять: **что доказано на данных**, **что взято из документа**, **что гипотеза**.
Гипотезу нумеровать и вносить в реестр контура, а не растворять в тексте. Отозванную
находку отзывать явно, отдельным коммитом (образец — PR #15).
